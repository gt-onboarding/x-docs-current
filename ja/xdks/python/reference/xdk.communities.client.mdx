---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

コミュニティに関する各種操作を行うための Client

<div id="constructors">
  ## コンストラクター
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

ID で Community を取得
指定した Community の ID を基に、その詳細情報を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.id" type="Any">
コミュニティの ID。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
表示するコミュニティフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="search">
  ### `search`
</div>

コミュニティを検索
指定した検索クエリに一致するコミュニティの一覧を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.query" type="str">
コミュニティ検索に使用するクエリ。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
1 回のリクエストで返される検索結果の最大件数。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
結果の次の「ページ」を取得するために使用するパラメーターです。このパラメーターの値は API から返されたレスポンスに含まれるものをそのまま使用し、変更しないでください。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の次の「ページ」を取得するために使用するパラメーターです。このパラメーターの値は API から返されたレスポンスに含まれるものをそのまま使用し、変更しないでください。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
表示するコミュニティフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchResponse`