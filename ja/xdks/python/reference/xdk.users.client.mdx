---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

ユーザー関連の操作を行う Client

<div id="constructors">
  ## コンストラクター
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

DM をブロックする
認証済みユーザーに対して、特定ユーザーの id を指定し、そのユーザーとの間で送受信されるダイレクトメッセージをブロックします。

#### パラメーター

<ParamField path="path.id" type="Any">
認証ユーザーがDMの受信をブロックするようリクエストしている対象ユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`BlockDmsResponse` - レスポンスデータ

<div id="create_bookmark">
  ### `create_bookmark`
</div>

ブックマークの作成
認証済みユーザーのブックマークにポストを追加します。

#### パラメーター

<ParamField path="path.id" type="Any">
ブックマークを追加する対象となる認証済みソースユーザーのid。
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
リクエストボディ
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

ブックマークの削除

認証済みユーザーのブックマークから、指定した id のポストを削除します。

#### パラメーター

<ParamField path="path.id" type="Any">
ブックマークを削除する認証済み送信元ユーザーのid。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
送信元ユーザーがブックマークから削除するポストのid。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteBookmarkResponse` - レスポンスデータ

<div id="follow_list">
  ### `follow_list`
</div>

リストをフォロー
認証済みユーザーが、指定したIDのリストをフォローします。

#### パラメーター

<ParamField path="path.id" type="Any">
リストをフォローする認証済み発信元ユーザーのid。
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
リクエストボディ
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

ユーザーをフォローする
認証済みユーザーが、指定したユーザーのidを持つユーザーをフォローします。

#### パラメーター

<ParamField path="path.id" type="Any">
ターゲットユーザーのフォローをリクエストしている、認証済みのソースユーザーの id。
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
リクエストボディ
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

ブロック中のユーザーを取得
指定されたユーザーIDがブロックしているユーザーの一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
特定の「ページ」の結果を取得するために使用するパラメータ。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

ブックマークフォルダーの取得
認証済みユーザーが作成したブックマークフォルダーの一覧を取得します。

#### パラメータ

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのIDです。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数です。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメータです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

ブックマークを取得
認証済みユーザーがブックマークしているポストのリストを取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返される結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次ページの結果を取得するために使用するパラメーター。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する投票フィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する場所フィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

フォルダーIDでブックマークを取得
認証済みユーザーの特定のブックマークフォルダーに含まれるポストを、そのフォルダーのIDで取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーの ID。
</ParamField>

<ParamField path="path.folder_id" type="Any">
認証済みユーザーがポストを取得しようとしているブックマークフォルダーの ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetBookmarksByFolderIdResponse` - レスポンスデータ

<div id="get_by_id">
  ### `get_by_id`
</div>

ID でユーザーを取得
指定したユーザー id の詳細情報を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得するユーザーのID。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドのカンマ区切りのリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りのリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストフィールドのカンマ区切りのリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="get_by_ids">
  ### `get_by_ids`
</div>

id でユーザーを取得する
複数のユーザーの詳細を、id を指定して取得します。

#### パラメーター

<ParamField path="path.ids" type="List">
カンマ区切りの User ID のリストです。最大 100 個まで指定できます。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストフィールドのカンマ区切りリストです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdsResponse` - レスポンスデータ

<div id="get_by_username">
  ### `get_by_username`
</div>

ユーザー名で User を取得する
指定したユーザー名に対応する User の詳細を取得します。

#### パラメーター

<ParamField path="path.username" type="str">
ユーザー名。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByUsernameResponse` - レスポンスデータ

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

ユーザー名でユーザーを取得する
複数のユーザーの詳細を、ユーザー名を指定して取得します。

#### パラメーター

<ParamField path="path.usernames" type="List">
カンマ区切りのユーザー名リスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストフィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByUsernamesResponse` - レスポンス データ

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

フォローしているリストを取得
指定したユーザーのidに基づいて、そのユーザーがフォローしているリストの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得するユーザーのIDです。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数です。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
指定した「ページ」の結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示するListのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

フォロワーの取得
特定のユーザーIDを指定して、そのユーザーをフォローしているユーザーのリストを取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得するユーザーのid。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返される結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
特定の「ページ」に対応する結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

フォロー中のユーザーを取得
指定したユーザーのIDを使って、そのユーザーがフォローしているユーザーの一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
取得するユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
指定した「ページ」の結果を取得するために使用するパラメータです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

いいねしたポストを取得
特定ユーザーがいいねしたポストのリストを、そのユーザーのIDを指定して取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得対象のユーザーID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメータ。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドを指定するカンマ区切りのリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを指定するカンマ区切りのリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドを指定するカンマ区切りのリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示するPollのフィールドを指定するカンマ区切りのリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドを指定するカンマ区切りのリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示するPlaceのフィールドを指定するカンマ区切りのリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

List メンバーシップを取得する
指定したユーザーの id を使って、そのユーザーがメンバーになっている複数の List を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
検索対象のユーザーのidです。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大件数です。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
特定の「ページ」の結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示するリストのフィールドをカンマ区切りで指定するリストです。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリストです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定するリストです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

自身のユーザー情報を取得
認証済みユーザーの詳細を取得します。

#### パラメーター

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetMeResponse` - レスポンスデータ

<div id="get_mentions">
  ### `get_mentions`
</div>

メンションを取得
特定のユーザーのidを指定して、そのユーザーへのメンションを含むポストの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得対象のユーザーのID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含めるポストIDの最小値。start_time も指定されている場合は、こちらが優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含めるポストIDの最大値。end_time も指定されている場合は、こちらが優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。ポストが返される最も早い UTC タイムスタンプ。since_id パラメーターも指定されている場合は、そちらが優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。ポストが返される最も遅い UTC タイムスタンプ。until_id パラメーターも指定されている場合は、そちらが優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll のフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User のフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place のフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

ミュートの取得
認証済みユーザーによってミュートされているユーザーの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数を指定します。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
このパラメーターは、次の「ページ」の結果を取得するために使用します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

所有するリストを取得
指定したユーザーのidを用いて、そのユーザーが所有しているリストの一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
照会対象ユーザーのIDです。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数です。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
指定した「ページ」の結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示するListフィールドをカンマ区切りで指定したリストです。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリストです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りで指定したリストです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

ピン留めされたリストを取得
認証済みユーザーがピン留めしているリストの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソース User の id。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示する List フィールドをカンマ区切りで指定した一覧。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定した一覧。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りで指定した一覧。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetPinnedListsResponse` - レスポンスデータ

<div id="get_posts">
  ### `get_posts`
</div>

ポストを取得
指定したユーザー ID を持つユーザーが作成したポストの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
検索対象のユーザーのID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含める最小のポストID。このパラメーターは、start_time が同時に指定されている場合、start_time より優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含める最大のポストID。このパラメーターは、end_time が同時に指定されている場合、end_time より優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
除外するエンティティの集合（例：「replies」や「retweets」）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。ポストを取得する最も早いUTCタイムスタンプ。since_id パラメーターが同時に指定されている場合は、since_id が優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。ポストを取得する最も遅いUTCタイムスタンプ。until_id パラメーターが同時に指定されている場合は、until_id が優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する投票のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する場所のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

自分のRepostを取得
認証されたユーザーのコンテンツをRepostしているポストのリストを取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.max_results" type="int or None" default="None">
返される結果の最大数を指定します。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

タイムラインを取得する
認証済みユーザーのタイムラインに含まれるポストを、逆時系列（新しい順）で取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
逆時系列タイムラインのポストを取得する対象となる、認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含める最小のポストID。両方が指定された場合、このパラメータは start_time より優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含める最大のポストID。両方が指定された場合、このパラメータは end_time より優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次のページの結果を取得するために使用されるパラメータです。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
除外するエンティティの集合（例: replies や retweets）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。ポストが返される最も早い UTC タイムスタンプ。since_id パラメータが同時に指定されている場合は、since_id が優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。ポストが返される最も遅い UTC タイムスタンプ。until_id パラメータが同時に指定されている場合は、until_id が優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet（ポスト）フィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する投票フィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する場所フィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

ポストに「いいね」する
認証済みユーザーが指定したIDの特定のポストに「いいね」を付けます。

#### パラメータ

<ParamField path="path.id" type="Any">
ポストへのいいねをリクエストしている認証済み送信元ユーザーのid。
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
リクエストボディ
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

ユーザーをミュート
認証済みユーザーが、指定したユーザーidのユーザーをミュートします。

#### パラメータ

<ParamField path="path.id" type="Any">
認証済みソースユーザーの id。ターゲットユーザーのミュートをリクエストしているユーザーです。
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
リクエストボディ
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

リストをピン留め
認証済みユーザーが、指定した ID のリストをピン留めします。

#### パラメーター

<ParamField path="path.id" type="Any">
リストをピン留めする認証済みソースユーザーのid。
</ParamField>

<ParamField path="body.body" type="PinListRequest">
リクエスト本文
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

ポストをリポストする
認証済みユーザーが、指定されたIDのポストをリポストします。

#### パラメータ

<ParamField path="path.id" type="Any">
ポストのリポストをリクエストしている、認証済みソースユーザーのID。
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
リクエストボディ
</ParamField>

<div id="search">
  ### `search`
</div>

ユーザーを検索
検索クエリに一致するユーザーの一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="Any">
ユーザーを検索するためのクエリ文字列。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大件数。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
このパラメータは、次の「ページ」の結果を取得するために使用します。指定する値は API が返すレスポンスから直接取得するものであり、変更してはいけません。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

DM のブロック解除
認証済みユーザーについて、指定したユーザーの id を指定して、そのユーザーとのダイレクトメッセージのブロックを解除します。

#### パラメータ

<ParamField path="path.id" type="Any">
認証済みユーザーがDMのブロック解除をリクエストしている相手ユーザーのid。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnblockDmsResponse` - レスポンスデータ

<div id="unfollow_list">
  ### `unfollow_list`
</div>

リストのフォロー解除
認証されたユーザーが、指定したIDの特定のリストのフォローを解除します。

#### パラメーター

<ParamField path="path.id" type="Any">
リストのフォローを解除する、認証済みのリクエスト元ユーザーのID。
</ParamField>

<ParamField path="path.list_id" type="Any">
フォローを解除するリストのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnfollowListResponse` - レスポンスデータ

<div id="unfollow_user">
  ### `unfollow_user`
</div>

ユーザーをフォロー解除
認証済みユーザーが、指定したユーザーのidを使ってそのユーザーのフォローを解除します。

#### パラメーター

<ParamField path="path.source_user_id" type="Any">
対象ユーザーのフォロー解除をリクエストしている認証済み送信元ユーザーのID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
送信元ユーザーがフォロー解除をリクエストしている対象ユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnfollowUserResponse` - レスポンスデータ

<div id="unlike_post">
  ### `unlike_post`
</div>

ポストの「いいね」を取り消す
認証済みユーザーが、特定の ID を持つポストの「いいね」を取り消します。

#### パラメータ

<ParamField path="path.id" type="Any">
ポストの「いいね」の取り消しをリクエストしている、認証済み送信元ユーザーのID。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
ユーザーが「いいね」の取り消しをリクエストしているポストのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnlikePostResponse` - レスポンスデータ

<div id="unmute_user">
  ### `unmute_user`
</div>

ユーザーのミュート解除
認証済みユーザーが、id を指定した特定のユーザーのミュートを解除します。

#### パラメーター

<ParamField path="path.source_user_id" type="Any">
ターゲットユーザーのミュート解除をリクエストしている、認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
ソースユーザーがミュート解除をリクエストしているユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnmuteUserResponse` - レスポンスデータ

<div id="unpin_list">
  ### `unpin_list`
</div>

リストのピン留めを解除
認証済みユーザーの特定のリストのピン留めを、その `id` を指定して解除します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.list_id" type="Any">
ピン留めを解除するListのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnpinListResponse` - レスポンスデータ

<div id="unrepost_post">
  ### `unrepost_post`
</div>

ポストのリポスト解除
認証済みユーザーが、指定したIDのポストのリポストを解除します。

#### パラメータ

<ParamField path="path.id" type="Any">
ポストのリポストをリクエストしている、認証済みの送信元ユーザーのID。
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
ユーザーがリポスト解除をリクエストしているポストのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnrepostPostResponse` - レスポンスデータ