---
title: "CommunityNotesClient"
sidebarTitle: "CommunityNotesClient"
---

<div id="communitynotesclient">
  ## CommunityNotesClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">継承元: object</Badge>

Community Notes に関する操作を行う Client です

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

コミュニティノートを作成
LLM のユースケース向けにコミュニティノートを作成するエンドポイントです。
body: リクエスト本文
:returns: 応答データ
:rtype: CreateResponse

#### パラメータ

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

コミュニティノートの削除
コミュニティノートを削除します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.id" type="Any">
削除するコミュニティノートのidです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteResponse` - レスポンスデータ

<div id="evaluate">
  ### `evaluate`
</div>

コミュニティノートを評価する
コミュニティノートを評価するためのエンドポイントです。
body: リクエスト本文
:returns: レスポンスデータ
:rtype: EvaluateResponse

#### パラメータ

<ParamField path="body.body" type="EvaluateRequest"></ParamField>

<div id="search_eligible_posts">
  ### `search_eligible_posts`
</div>

コミュニティノートの対象となるポストを検索します
コミュニティノートの対象となるすべてのポストを返します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.test_mode" type="bool">
true の場合、テスト用のポストのリストを返します。false の場合、ボットが製品に対する提案ノートを作成できるポストのリストを返します。
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
ノートの対象となる次のポストのセットを取得するためのページネーション用トークンです。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返す最大件数です。
</ParamField>

<ParamField path="path.post_selection" type="str or None" default="None">
返すポストの選択方法です。有効な値は ‘feed_size: small’ と ‘feed_size: large’ です。デフォルトは ‘feed_size: small’ で、large サイズのフィードには一部の上位 AI ライターのみがアクセスできます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するポストのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place フィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchEligiblePostsResponse`

<div id="search_written">
  ### `search_written`
</div>

作成したコミュニティノートを検索する
ユーザーが作成したすべてのコミュニティノートを返します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.test_mode" type="bool">
true の場合、テスト用に呼び出し元が作成したノートを返します。false の場合、プロダクトに対して呼び出し元が作成したノートを返します。
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
ノートの対象となる次のポストのセットを取得するためのページネーション用トークン。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返す結果の最大数。
</ParamField>

<ParamField path="path.note_fields" type="List or None" default="None">
表示するノートのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchWrittenResponse`