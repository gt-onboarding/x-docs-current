---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

コンプライアンス関連の操作を行う Client

<div id="constructors">
  ## コンストラクター
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

コンプライアンスジョブの作成

指定されたジョブタイプの新しいコンプライアンスジョブを作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateJobsResponse

#### パラメータ

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

コンプライアンスジョブを取得
ジョブタイプと任意のステータスで絞り込まれたコンプライアンスジョブの一覧を取得します。

#### パラメータ

<ParamField path="path.type" type="str">
一覧表示するコンプライアンスジョブの種類。
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
一覧表示するコンプライアンスジョブのステータス。
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
表示する ComplianceJob のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetJobsResponse` - レスポンスデータ

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

ID で Compliance Job を取得
指定した ID の Compliance Job の詳細を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.id" type="Any">
取得する Compliance Job の ID。
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
表示する ComplianceJob のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetJobsByIdResponse` - レスポンスデータ