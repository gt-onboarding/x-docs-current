---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

Webhook 操作用の Client

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Webhook を作成
新しい Webhook 構成を作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateResponse

#### パラメータ

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

ストリームリンクを作成する
指定した webhook に FilteredStream イベントを配信するためのリンクを作成します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_id" type="Any">
FilteredStream のルールセットにリンクする webhook の ID。
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
表示するポストのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
表示するメディアのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
表示する Poll のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
表示する Place のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`CreateStreamLinkResponse` - レスポンスデータ

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

Webhook 用のリプレイジョブを作成
Webhook に配信された、または配信を試行したすべてのイベントについて、過去最大 24 時間分のイベントを取得するリプレイジョブを作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateWebhookReplayJobResponse

#### パラメータ

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

webhook の削除
既存の webhook 設定を削除します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_id" type="Any">
削除対象の webhook の id。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteResponse` - レスポンスデータ

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

ストリームリンクを削除
指定した webhook に対する FilteredStream イベントのリンクを削除します。

#### パラメータ

<ParamField path="path.webhook_id" type="Any">
FilteredStream のルールセットに関連付ける webhook の id。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteStreamLinkResponse` - レスポンスデータ

<div id="get">
  ### `get`
</div>

Webhook の取得
クライアントアプリに関連付けられている webhook 設定の一覧を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
表示するWebhookConfigのフィールドをカンマ区切りのリストとして指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetResponse` - レスポンスデータ

<div id="get_stream_links">
  ### `get_stream_links`
</div>

ストリームのリンクを取得します  
filtered stream ルールセットに関連付けられている webhook リンクの一覧を取得します。
:returns: レスポンスデータ
:rtype: GetStreamLinksResponse

<div id="returns">
  #### 戻り値
</div>

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

Webhook を検証します
指定した Webhook に対して CRC チェックを実行します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_id" type="Any">
確認するWebhookのid。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`ValidateResponse` - 検証結果のレスポンスデータ