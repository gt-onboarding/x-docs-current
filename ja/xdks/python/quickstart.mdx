---
title: クイックスタート
sidebarTitle: クイックスタート
---

このクイックスタートでは、Bearer Token 認証を使用して XDK を使い、ポストを簡単に検索する方法を紹介します。

<div id="step-1-install-the-sdk">
  ## ステップ 1: SDK をインストールする
</div>

```bash
pip install xdk
```

<div id="step-2-get-your-bearer-token">
  ## ステップ 2: Bearer Token を取得する
</div>

1. [X Developer Portal](https://developer.x.com/en/portal/dashboard) にログインします。
2. アプリを新規作成するか、既存のアプリを選択します。
3. 「Keys and Tokens」セクションで、Bearer Token（アプリのみ認証）を生成します。

<div id="step-3-write-and-run-your-first-script">
  ## ステップ3：最初のスクリプトを作成して実行する
</div>

`quickstart.py` という名前のファイルを作成します。

```python
# Clientをインポート
from xdk import Client
# 実際のBearer Tokenに置き換えてください
client = Client(bearer_token="YOUR_BEARER_TOKEN_HERE")
# "api"に言及している最近のポストを取得
# search_recentはIteratorを返すので、それを反復処理します
for page in client.posts.search_recent(query="api", max_results=10):
    if page.data and len(page.data) > 0:
        # 最初のポストにアクセス - Pydanticモデルは属性とdict両方のアクセスをサポート
        first_post = page.data[0]
        post_text = first_post.text if hasattr(first_post, 'text') else first_post.get('text', '')
        print(f"Latest Post: {post_text}")
        break
    else:
        print("No Posts found.")
        break
```

実行してみましょう：

```bash
python quickstart.py
```

**期待される出力**:

```
Latest Post: Exciting updates on XDK Python SDK!
```

**トラブルシューティング**: 401 エラーが発生した場合は、Bearer Token をもう一度確認してください。レート制限 (429) エラーの場合は、しばらく待ってから再試行してください。

<div id="next-steps">
  ## 次のステップ
</div>

* [認証](/ja/xdks/python/authentication)を参照して、Bearer Token（アプリのみ）認証、PKCE を用いた OAuth 2.0（ユーザーコンテキスト）、および OAuth 1.0a（レガシーなユーザーコンテキスト）の使い方を理解しましょう。
* 多数の結果を一度に取得したいが、複数回の API 呼び出しをあまり意識したくないユースケース向けに、[ページネーション](/ja/xdks/python/pagination)について学びましょう。
* リアルタイムデータを扱う方法を学ぶために、[ストリーミング](/ja/xdks/python/streaming)を詳しく見てみましょう。
  Python XDK を使った詳細なコード例については、[コードサンプル GitHub リポジトリ](https://github.com/xdevplatform/samples/tree/main/python)を参照してください。