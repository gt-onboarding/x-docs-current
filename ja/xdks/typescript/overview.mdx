---
title: "TypeScript XDK"
sidebarTitle: "概要"
---

X API（旧 Twitter API）向けの包括的な TypeScript SDK です。スマートなページネーション、多様な認証方式、リアルタイムストリーミング、完全な型安全性などの高度な機能を備えています。

<div id="key-features">
  ## 主な機能
</div>

* **🔐 Authentication**: User Context（OAuth1.0a, OAuth2.0）および App-Only（Bearer Token）認証
* **🔄 Pagination**: 非同期イテレーションに対応した自動ページネーション
* **📡 Streaming**: 自動再接続付きのイベント駆動型ストリーミング
* **📚 Type Safety**: すべてのエンドポイントとパラメータに対する完全な TypeScript 定義
* **🎯 Full X API Support**: Users、Posts、Lists、Bookmarks、Communities など、X API 機能を完全にサポート

<div id="quick-start">
  ## クイックスタート
</div>

<CodeGroup dropdown>
  ```typescript quickstart.ts theme={null}
  import { 
      Client, 
      type ClientConfig,
      type Users
  } from '@xdevplatform/xdk';

  const config: ClientConfig = { bearerToken: 'your-bearer-token' };

  const client: Client = new Client(config);

  async function main(): Promise<void> {
    const userResponse: Users.GetByUsernameResponse = await client.users.getByUsername('XDevelopers');
    const username: string = userResponse.data?.username!;
    console.log(username);
  }

  main();
  ```

  ```javascript quickstart.js theme={null}
  import { Client } from '@xdevplatform/xdk';

  const client = new Client({ bearerToken: 'your-bearer-token' });

  const userResponse = await client.users.getByUsername('XDevelopers');
  const username = userResponse.data.username;
  console.log(username);
  ```
</CodeGroup>

<Info>
  JavaScript/TypeScript XDK を使用した詳細なコード例については、[コードサンプルの GitHub リポジトリ](https://github.com/xdevplatform/samples/tree/main/javascript)を参照してください。
</Info>

<div id="whats-next">
  ## 次のステップ
</div>

* [Installation Guide](/ja/xdks/typescript/install) - SDK をプロジェクトにセットアップする
* [Authentication](/ja/xdks/typescript/authentication) - さまざまな認証方法について学ぶ
* [Pagination](/ja/xdks/typescript/pagination) - データのページネーションについて学ぶ
* [Streaming](/ja/xdks/typescript/streaming) - リアルタイムデータのストリーミングについて学ぶ
* [API Reference](/ja/xdks/typescript/reference/Client) - 完全な API ドキュメントを参照する