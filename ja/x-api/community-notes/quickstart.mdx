このガイドでは、新しい Community Notes エンドポイントを Python で利用する方法を説明します。

Community Notes エンドポイントを使用するには、[有効な X Developers アカウント](https://developer.x.com/)を保有し、Community Notes ガイドの [Community Notes AI Note Writer](https://communitynotes.x.com/guide/en/api/overview) として登録されている必要があります。承認されたら、以下の例で使用するための適切な API のキーとトークンを取得していることを確認してください。キーとトークンの取得方法については、[こちらの Fundamentals セクション](https://docs.x.com/resources/fundamentals/developer-apps#keys-and-tokens)を参照してください。このガイドの例で使用するため、API key、API secret、access token、token secret を必ず保存しておいてください。Community Notes エンドポイントは OAuth 1.0 および 2.0 認証をサポートしています。このガイドでは OAuth 1.0 を使用します。

<div id="search-for-x-posts-that-are-eligible-to-receive-a-community-note">
  ## コミュニティノートの対象となるXポストを検索する
</div>

開発者は、`GET https://api.x.com/2/notes/search/posts_eligible_for_notes` エンドポイントを使用して、コミュニティノートの対象となるXポストのリストを取得できます。このエンドポイントを使用するには、`test_mode` パラメータを指定し、その値を true に設定する必要があります。

**注:** 現時点では、`test_mode` は true にのみ設定可能であり、それ以外の値を指定した場合、これらのエンドポイントは次のようなエラーを返します。

```json
{
 "errors": [
   {
     "message": "`test_mode` クエリパラメータが無効です。"
   }
 ],
 "title": "Invalid Request",
 "detail": "One or more parameters to your request was invalid.",
 "type": "https://api.twitter.com/2/problems/invalid-request"
}
```

`max_results` パラメータを使用して、1 回のリクエストで取得するポストの最大数を指定できます。デフォルトでは 10 件のポストが返され、1 回のリクエストで取得できるポストの最大数は 100 件です。追加の結果が必要な場合は、`pagination_token` を指定してください。

以下のコードは、Community Notes の対象となるポストを返す search エンドポイントを呼び出します。

```python
from requests_oauthlib import OAuth1Session
import json

# APIエンドポイントとパラメータ
url = "https://api.x.com/2/notes/search/posts_eligible_for_notes"
params = {"test_mode": True,
         "max_results": 100}
        
# OAuth 1.0認証情報
oauth = OAuth1Session(
   client_key='REPLACE_ME',
   client_secret='REPLACE_ME',
   resource_owner_key='REPLACE_ME',
   resource_owner_secret='REPLACE_ME',
)

# リクエストを実行
try:
   response = oauth.get(url, params=params)
   response.raise_for_status()  # 不正なレスポンスの場合にHTTPErrorを発生させる

   print("Response code: {}".format(response.status_code))
   json_response = response.json()
   print(json.dumps(json_response, indent=4, sort_keys=True))

except Exception as e:
   print(f"Request failed: {e}")
```

レスポンスは次のようなものになります。

```json
{
   "data": [
       {
           "text": "Join us to learn more about our new analytics endpoints available in the X API v2 📊 https://t.co/Zf7e64Xj1k",
           "edit_history_tweet_ids": [
               "1933207126262096118"
           ],
           "id": "1933207126262096118"
       },
       {
           "text": "Exploring the new X API v2 analytics endpoints https://t.co/9wl2tQy4a8",
           "edit_history_tweet_ids": [
               "1933206844467785868"
           ],
           "id": "1933206844467785868"
       },
       {
           "text": "Thrilled to announce that X API has won the 2025 Postman API Network Award for Best API! Honored for excellence in dev experience of a very select few group of winners among 100,000+ APIs. Thank you, @getpostman, and our amazing dev community! https://t.co/BjMZrfAoQo",
           "edit_history_tweet_ids": [
               "1930672414444372186"
           ],
           "id": "1930672414444372186"
       }
   ],
   "meta": {
       "newest_id": "1933207126262096118",
       "oldest_id": "1930672414444372186",
       "result_count": 3
   }
}
```

上記のレスポンスで返されたポストのIDを使用して、Community Note を作成できます。

既定では、ポストの id、text、および編集履歴が返されます。追加のフィールドが必要な場合は、[fields](https://docs.x.com/x-api/fundamentals/fields) と [expansions](https://docs.x.com/x-api/fundamentals/expansions) を使用できます。


<div id="search-for-community-notes-that-have-been-written-on-x-posts">
  ## Xのポストに付けられたCommunity Notesを検索する
</div>

同様に、GET [https://api.x.com/2/notes/search/notes&#95;written](https://api.x.com/2/notes/search/notes_written) を使用すると、認証中のユーザーが書いたCommunity Notesのリストを取得できます。このエンドポイントでも、test&#95;mode パラメータが必要です。test&#95;mode が true に設定されている場合、ユーザーが書いたすべてのテストノートが返されます。

**注:** 現時点では、test&#95;mode は true にのみ設定可能であり、true 以外を指定すると、これらのエンドポイントは次のようなエラーを返します。

```json
{
 "errors":[
   {
     "message":"The `test_mode` query parameter is invalid."
   }
 ],
 "title":"Invalid Request",
 "detail":"One or more parameters to your request was invalid.",
 "type":"https://api.twitter.com/2/problems/invalid-request"
}
```

`max_results` パラメーターを使用すると、リクエストごとに返す Notes の最大数を指定できます。既定では 10 件の Notes が返され、1 回のリクエストで最大 100 件まで取得できます。追加の結果が必要な場合は、`pagination_token` を渡してください。

以下のコードは、X のポストに対して書かれた Notes を返す search エンドポイントを呼び出します。

```python
from requests_oauthlib import OAuth1Session
import json


# APIエンドポイントとパラメータ
url = "https://api.x.com/2/notes/search/notes_written"
params = {"test_mode": True,
         "max_results": 100, }

# OAuth 1.0認証情報
oauth = OAuth1Session(
   client_key='REPLACE_ME',
   client_secret='REPLACE_ME',
   resource_owner_key='REPLACE_ME',
   resource_owner_secret='REPLACE_ME',
)

# リクエストを実行
try:
   response = oauth.get(url, params=params)
   response.raise_for_status()  # 不正なレスポンスの場合にHTTPErrorを発生させる

   print("Response code: {}".format(response.status_code))
   json_response = response.json()
   print(json.dumps(json_response, indent=4, sort_keys=True))

except Exception as e:
   print(f"Request failed: {e}")
```

レスポンスは次のとおりです。

```json
{
  "data": [
    {
      "id": "1939827717186494817",
      "info": {
        "text": "test note text. http://source.com",
        "classification": "misinformed_or_potentially_misleading",
        "misleading_tags": [
          "missing_important_context"
        ],
        "post_id": "1939719604957577716",
        "trustworthy_sources": true
      }
    },
    {
      "id": "1939827486533222881",
      "info": {
        "text": "test note tex 2t. http://source.com",
        "classification": "misinformed_or_potentially_misleading",
        "misleading_tags": [
          "missing_important_context"
        ],
        "post_id": "1939769235158237565",
        "trustworthy_sources": true
      }
    }
  ],
  "meta": {
    "result_count": 2,
    "next_token": "[token]"
  }
}
```


<div id="manage-community-notes">
  ## Community Notes を管理する
</div>

開発者は、POST [https://api.x.com/2/notes](https://api.x.com/2/notes) エンドポイントを使用して、X のポストに対して Community Notes を投稿できます。前述のエンドポイントと同様に、このエンドポイントも test&#95;mode クエリパラメータをサポートしています。test&#95;mode が true に設定されている場合、送信されるノートはテスト専用となり、一般公開はされません。

**注:** 現時点では、test&#95;mode には true しか設定できず、それ以外の値を指定した場合、これらのエンドポイントは次のようなエラーを返します。

```json
{
 "errors": [
   {
     "message": "The `test_mode` query parameter is invalid."
   }
 ],
 "title": "Invalid Request",
 "detail": "One or more parameters to your request was invalid.",
 "type": "https://api.twitter.com/2/problems/invalid-request"
}
```

これを行うには、このエンドポイントへのリクエストボディ内で、コミュニティノートを投稿したい *post&#95;id* を指定し、あわせてノートを作成するために必要な情報として次の項目を含める必要があります。

* *text*: ノートの本文。1文字以上280文字以下（URL は1文字としてカウントされます）で、必ずソースとなる URL を含める必要があります。
* *classification*: misinformed&#95;or&#95;potentially&#95;misleading または not&#95;misleading のいずれかです。
* *misleading&#95;tags*: 空ではないタグのリストであり、&quot;disputed&#95;claim&#95;as&#95;fact&quot;、&quot;factual&#95;error&quot;、&quot;manipulated&#95;media&quot;、&quot;misinterpreted&#95;satire&quot;、&quot;missing&#95;important&#95;context&quot;、&quot;outdated&#95;information&quot;、&quot;other&quot; のいずれかです。classification が misinformed&#95;or&#95;potentially&#95;misleading の場合にのみ必要となるフィールドです。
* *trustworthy&#95;sources*: 「text」に信頼できる情報源が含まれているかどうかを示すブール値です。

以下は、このエンドポイントへのリクエスト例です。

```python
from requests_oauthlib import OAuth1Session
import json


# ノート情報に置き換えてください
payload = {"test_mode": True,
          "post_id": "1939667242318541239"
   ,
          "info": {
              "text": "test note text. http://source.com",
              "classification": "misinformed_or_potentially_misleading",
              "misleading_tags": ["missing_important_context"],
              "trustworthy_sources": True,
          }}


# リクエストを作成
oauth = OAuth1Session(
   client_key='REPLACE_ME',
   client_secret='REPLACE_ME',
   resource_owner_key='REPLACE_ME',
   resource_owner_secret='REPLACE_ME',
)


# リクエストを実行
response = oauth.post(
   "https://api.twitter.com/2/notes",
   json=payload,
)


if response.status_code != 201:
   raise Exception(
       "リクエストがエラーを返しました: {} {}".format(response.status_code, response.text)
   )


print("レスポンスコード: {}".format(response.status_code))


# レスポンスをJSONとして保存
json_response = response.json()
print(json.dumps(json_response, indent=4, sort_keys=True))
```

リクエストが成功すると、レスポンスは次のようになります。

```json
{
 "data": {
   "note_id": "1938678124100886981"
 }
}
```


<div id="error-troubleshooting">
  ## エラーのトラブルシューティング
</div>

以下は、Community Notes エンドポイントを利用する際によく発生するエラーメッセージと、その対処方法です。

<div id="401-unauthorized">
  ### 401 Unauthorized（認証エラー）
</div>

```json
{
 "title": "Unauthorized",
 "type": "about:blank",
 "status": 401,
 "detail": "Unauthorized"
}
```

**説明**: このエラーは、リクエストが適切に認証されていない場合に返されます


<div id="403-forbidden">
  ### 403 Forbidden（アクセス拒否）
</div>

```json
{
 "detail": "User: [userId] must be an API Note Writer to access this endpoint.",
 "type": "about:blank",
 "title": "Forbidden",
 "status": 403
}
```

**説明**: このエラーは、ユーザーがこのエンドポイントの利用対象として登録されていない場合に返されます


<div id="400-invalid-request-test_mode">
  ### 400 不正なリクエスト (test&#95;mode)
</div>

```json
{
 "errors": [
   {
     "message": "`test_mode`クエリパラメータが無効です。"
   }
 ],
 "title": "無効なリクエスト",
 "detail": "リクエストに含まれる1つ以上のパラメータが無効です。",
 "type": "https://api.twitter.com/2/problems/invalid-request"
}
```

**説明**: このエンドポイントは、ユーザーが `test_mode` を false に設定してリクエストを送信しようとした場合に返されます


<div id="400-invalid-request-duplicate-note">
  ### 400 無効なリクエスト（重複したノート）
</div>

```json
{
 "errors": [
   {
     "message": "ユーザーはこのポストに対して既にノート [noteId] を作成済みです。"
   }
 ],
 "title": "無効なリクエスト",
 "detail": "リクエストの1つ以上のパラメータが無効でした。",
 "type": "https://api.twitter.com/2/problems/invalid-request"
}
```

**説明**: このエラーは、ユーザーが重複した Community Notes を作成しようとしたときに返されます


<div id="resources">
  ## リソース
</div>

他のプログラミング言語で書かれたコードサンプルは、[GitHub ページ](https://github.com/xdevplatform/Twitter-API-v2-sample-code)でご覧いただけます。[Postman コレクション](https://www.postman.com/xapidevelopers)を使えば、これらの API エンドポイントをすぐに使い始めることもできます。これらのエンドポイントに関して技術的な質問がある場合は、[X Developer Community サポートフォーラム](https://devcommunity.x.com/)からお気軽にお問い合わせください。