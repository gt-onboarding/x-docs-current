---
title: ピン留めされたリストを管理する
sidebarTitle: ピン留めされたリストを管理する
keywords: ["ピン留めされたリストの管理クイックスタート", "リストのピン留めクイックスタート", "リストのピン留め解除クイックスタート", "ピン留めされたリストの管理"]
---

import { Button } from '/snippets/ja/button.mdx';

<Note>
  **注意:** このガイドでは、[クイックスタート概要](/ja/x-api/lists/pinned-lists#getting-started-with-the-pinned-list-endpoint-group)で説明している前提条件をすべて完了していることを想定しています。
</Note>

<div id="steps-to-build-a-manage-pinned-list-request">
  ### ピン留めされた List を管理するリクエストを構築する手順
</div>

**ステップ 1: Postman で List エンドポイントコレクションを選択する**

Postman に X API v2 コレクションを読み込んだら、「List」フォルダーに移動し、「Pinned Lists」フォルダーを選択してから、「Pin a List」を選択します。
 

**ステップ 2: ピン留めする List を指定する**

ピン留めされた List を管理するエンドポイントには 2 つの ID が必要です。1 つはユーザー（List をピン留めする認証済みユーザー）の ID、もう 1 つは対象の List（ピン留めされる List）の ID です。ユーザーの ID は、認証に使用するユーザーの ID と一致している必要があります。つまり、リクエストを認証する際には、そのユーザー ID に関連付けられたアクセストークンを渡す必要があります。この場合、自分のユーザー ID を指定できます。自分の ID は次の 2 通りの方法で確認できます。

1. [users lookup](/ja/x-api/users/lookup/introduction) by username エンドポイントを使用して、ユーザー名を渡すことで `id` フィールドを取得できます。
2. アクセストークンを確認すると、その数値部分があなたのユーザー ID になっています。

対象の List ID は、有効な List であればどれでも構いません。Postman で「Params」タブに移動し、id パス変数の「Value」列にユーザー ID を入力します。次に「Body」タブに移動し、ピン留めしたい List の ID を list&#95;id パラメーターの値として入力します。いずれの ID の前後にもスペースが入らないように注意してください。

|             |                     |                    |
| :---------- | :------------------ | :----------------- |
| **Key**     | **Value**           | **Parameter type** |
| `id`        | ユーザー ID             | path               |
| list&#95;id | ピン留め対象となる List の ID | body               |

これで、「Send」ボタンの横に次のような URL が表示されているはずです。

```
      https://api.x.com/2/users/2244994945/pinned_lists
```

ステップ3: リクエストを送信してレスポンスを確認する

すべての準備が整ったら、「Send」ボタンをクリックします。すると、次のサンプルレスポンスと同様のレスポンスが返ってきます。

```
{
  "data": {
    "pinned": true
  }
}
```

返されたレスポンスオブジェクト内で、キー `pinned` に対して `true` の値が含まれていれば、対象のリストは正常にピン留めされたことになります。 

リストのピン留めを解除するには、Postman に読み込んだ X API v2 コレクションの「Lists」フォルダー内にある「Unpin List」リクエストを選択します。このエンドポイントには、認証済みユーザーの ID と、ピン留めを解除するリストの ID が必要です。「Params」タブで、`id` 列の値としてユーザー ID を、`list_id` 列の値としてピン留めを解除するリストの ID を入力します。 

削除のリクエストが成功すると、次の例のようなレスポンスが返されます。

```
{
  "data": {
    "pinned": false
  }
}
```
