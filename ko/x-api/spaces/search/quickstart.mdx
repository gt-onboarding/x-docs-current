---
title: 빠른 시작
sidebarTitle: 빠른 시작
keywords: ["Spaces 검색 빠른 시작", "Spaces 검색 시작하기", "Spaces 검색 튜토리얼", "Spaces 검색 가이드"]
---

import { Button } from '/snippets/ko/button.mdx';

<div id="getting-started-with-the-search-spaces-endpoint">
  ## search Spaces 엔드포인트 시작하기
</div>

이 빠른 시작 가이드는 Postman을 사용하여 지정한 필드 집합과 함께 search Spaces 엔드포인트에 첫 요청을 보내는 방법을 설명합니다.

다른 프로그래밍 언어의 샘플 코드를 보고 싶다면 [X API v2 sample code GitHub repository](https://github.com/xdevplatform/Twitter-API-v2-sample-code)를 방문하세요.

<Note>
  **사전 준비 사항**

  이 가이드를 완료하려면 요청을 인증하기 위한 [키와 토큰](/ko/resources/fundamentals/authentication) 세트가 필요합니다. 다음 단계를 따르면 이러한 키와 토큰을 생성할 수 있습니다:

  * [개발자 계정에 가입](https://developer.x.com/en/apply-for-access)하고 승인을 받습니다.
  * 개발자 포털에서 [Project](/ko/resources/fundamentals/projects)와 연결된 [developer App](/ko/resources/fundamentals/developer-apps)을 생성합니다.
  * 앱의 &quot;Keys and tokens&quot; 페이지로 이동하여 필요한 자격 증명을 생성합니다. 모든 자격 증명을 안전한 위치에 저장했는지 확인하세요.
</Note>

<div id="steps-to-build-a-search-spaces-request">
  ### Spaces 검색 요청을 구성하는 단계
</div>

**1단계: 도구 또는 라이브러리 선택하기**

이 엔드포인트에 요청을 보내기 위해 사용할 수 있는 다양한 도구, 코드 예제, 라이브러리가 있지만, 여기에서는 과정을 단순화하기 위해 Postman 도구를 사용하겠습니다.

X API v2 Postman 컬렉션을 환경에 불러오려면 아래 버튼을 클릭하세요:

<Button href="https://app.getpostman.com/run-collection/9956214-784efcda-ed4c-4491-a4c0-a26470a67400">
  X API v2를 Postman에 추가
</Button>

Postman에 X API v2 컬렉션을 불러온 후, Spaces 폴더로 이동하여 &quot;Search Spaces&quot; 요청을 찾습니다.
 

**2단계: 요청 인증하기**

X API에 정상적으로 요청을 보내려면 권한이 있는지 확인해야 합니다. 이를 위해 이 엔드포인트에서는 [OAuth 2.0 App-Only](/ko/resources/fundamentals/authentication#bearer-token-also-known-as-app-only) 또는 [OAuth 2.0 Authorization Code with PKCE](/ko/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) 인증 방식을 사용하여 요청을 인증해야 합니다.

단순화를 위해 이 예제에서는 OAuth 2.0 App-Only를 사용하겠지만, 비공개 [metrics](/ko/x-api/fundamentals/metrics) 또는 비공개 사용자의 Spaces를 요청하려면 다른 인증 방식 중 하나를 사용해야 합니다. 

OAuth 2.0 App-Only를 사용하려면, 특히 [App Access Token](/ko/resources/fundamentals/authentication#bearer-token-also-known-as-app-only) (App-only Bearer Token이라고도 부르는)을 포함한 키와 토큰을 Postman에 추가해야 합니다. 이를 위해 Postman 우측 상단에서 “X API v2”라는 환경을 선택한 다음, 환경 드롭다운 옆의 눈 모양 아이콘을 클릭하여 &quot;initial value&quot; 및 &quot;current value&quot; 필드에 키와 토큰을 추가합니다.

올바르게 설정했다면 이 변수들은 요청의 Authorization 탭에 자동으로 반영됩니다.
 

**3단계: 검색 쿼리 생성하기**

이 엔드포인트는 검색 쿼리로 텍스트를 입력받습니다. 다른 검색 엔드포인트와는 달리, 연산자, 그룹화, 논리 연산자는 허용하지 않습니다. 이 연습에서는 간단한 쿼리로 “hello”를 사용하겠습니다.

Postman에서 &quot;Params&quot; 탭으로 이동한 뒤, id 파라미터의 &quot;Value&quot; 열에 이 사용자 ID를 입력합니다.

|         |           |
| :------ | :-------- |
| **Key** | **Value** |
| query   | hello     |

**4단계: 가져오려는 필드 식별 및 지정하기**

3단계 이후에 &quot;Send&quot; 버튼을 클릭하면 응답에는 기본적으로 Space 객체 필드 중 id와 state만 반환됩니다.

추가 필드를 받고 싶다면, 요청에서 space.fields 또는 expansions 파라미터를 사용해 이를 지정해야 합니다.

이 연습에서는 서로 다른 객체에서 세 개의 추가 필드 집합을 요청하겠습니다:

* 기본 Spaces 객체에서 추가 title 필드
* 지정된 creator ID의 전체 사용자 객체
* 연결된 사용자 객체에서 추가 user.created&#95;at 필드

Postman에서 “Params” 탭으로 이동한 후, “Query Params” 테이블에 다음 key:value 쌍을 추가합니다:

|              |                |                                                                 |
| :----------- | :------------- | :-------------------------------------------------------------- |
| **Key**      | **Value**      | **Returned fields**                                             |
| space.fields | title          | creator&#95;id                                                  |
| expansions   | creator&#95;id | includes.users.id, includes.users.name, includes.users.username |
| user.fields  | created&#95;at | includes.users.created&#95;at                                   |

이제 “Send” 버튼 옆에 다음 URL이 표시되어야 합니다:

[https://api.x.com/2/spaces/search?query=hello&amp;space.fields=creator&#95;id&amp;expansions=creator&#95;id&amp;user.fields=created&#95;at](https://api.x.com/2/spaces/search?query=hello\&space.fields=creator\\_id\&expansions=creator\\_id\&user.fields=created_at)

**5단계: 요청 보내고 응답 확인하기**

모든 설정이 완료되면 “Send” 버튼을 클릭하면 되고, 그러면 다음과 같은 응답을 받게 됩니다:

```{
   "data": [
    {
        "creator_id": "2244994945",
        "id": "1zqKVXPQhvZJB",
        "title": "Hello world 👋",
        "state": "Running"
   },
   "includes": {
       "users": [
           {
               "created_at": "2013-12-14T04:35:55.000Z",
               "name": "Twitter Dev",
               "id": "2244994945",
               "username": "TwitterDev"
           }
       ]
   }
]
}
```
