---
title: "필드"
keywords: ["fields", "tweet fields", "user fields", "field parameters", "API fields", "data fields", "customize response", "field selection"]
---

X API v2 엔드포인트에는 *필드*라고 불리는 일련의 매개변수가 제공되며, 이를 통해 엔드포인트 응답에 포함된 각 객체에서 원하는 데이터만 선택할 수 있습니다.

기본적으로 게시물 객체는 `id`와 `text` 필드만 반환하며, 2022년 9월 29일 이후에 생성된 게시물의 경우 `edit_history_tweet_ids` 필드도 반환합니다. 게시물의 작성 날짜나 공개 메트릭이 필요하다면 이를 요청하기 위해 `tweet.fields` 매개변수를 사용해야 합니다. 이렇게 하면 사용 사례에 따라 필요한 필드만 요청할 수 있어 응답을 훨씬 더 세밀하게 맞춤 설정할 수 있습니다. 예를 들어, 요청에 다음과 같은 쿼리 문자열을 포함할 수 있습니다:

`?tweet.fields=created_at,public_metrics`

각 객체에는 해당 객체와 연결된 필드를 명시적으로 요청하는 데 사용되는 고유한 매개변수가 있습니다. 현재 사용 가능한 다양한 필드 매개변수는 다음과 같습니다:

* [Post](/ko/x-api/fundamentals/data-dictionary#tweet) → `tweet.fields`
* [User](/ko/x-api/fundamentals/data-dictionary#user) → `user.fields`
* [Media](/ko/x-api/fundamentals/data-dictionary#media) → `media.fields`
* [Poll](/ko/x-api/fundamentals/data-dictionary#poll) → `poll.fields`
* [Place](/ko/x-api/fundamentals/data-dictionary#place) → `place.fields`

특정 객체를 주로 반환하는 엔드포인트를 사용할 때는, 해당 객체에 대응하는 필드 매개변수를 사용하고 원하는 필드를 쉼표로 구분한 목록 형태로 그 매개변수의 값에 지정하면, 응답에서 해당 필드들을 함께 받아볼 수 있습니다.

<div id="example">
  ### 예시
</div>

[GET /tweets/search/recent](/ko/x-api/posts/recent-search) 엔드포인트를 사용하면 해당 응답에서는 주로 [Tweet objects](/ko/x-api/fundamentals/data-dictionary#tweet)를 받게 됩니다. 별도의 fields 파라미터를 지정하지 않으면 기본값인 `id`와 `text`만 받게 됩니다. 응답에 반환되는 게시물의 공개 메트릭을 받고 싶다면, 요청에 `tweet.fields` 파라미터를 포함하고 그 값으로 `public_metrics`를 설정해야 합니다.

이 요청은 다음과 같은 형태입니다. 이 요청을 사용하려면 `$BEARER_TOKEN`을 자신의 [Bearer Token](/ko/resources/fundamentals/authentication)으로 바꾼 후, 사용 중인 명령줄 도구를 통해 전송해야 합니다.

```bash
curl --request GET \
  --url 'https://api.x.com/2/tweets/search/recent?query=from%3Atwitterdev&tweet.fields=public_metrics' \
  --header 'Authorization: Bearer $BEARER_TOKEN'
```

터미널에서 이 요청을 실행하면, 응답으로 반환되는 각 게시물에는 다음 필드가 포함됩니다:

```
{
   "data": {
       "id": "1263150595717730305",
       "public_metrics": {
           "retweet_count": 12,
           "reply_count": 14,
           "like_count": 49,
           "quote_count": 7
       },
       "text": "새로운 트윗 설정을 👀 보셨나요?\n\nAPI에서 이와 같은 기능을 어떻게 그리고 왜 사용하실지 알고 싶습니다. 자세한 내용을 확인하시고 의견을 알려주세요👇\nhttps://t.co/RtMhhfAcIB https://t.co/8wxeZ9fJER"
   }
}
```

엔드포인트가 반환하는 기본 객체와 연관된 보조 객체의 특정 필드 집합을 가져오려면 추가적인 expansions 파라미터를 포함해야 합니다. 예를 들어, 앞에서 사용했던 동일한 GET search/tweets/recent 엔드포인트를 사용하면서 작성자의 프로필 설명을 가져오려면 요청에 `expansions=author_id` 및 `user.fields=description`를 함께 전달해야 합니다. 아래는 이 요청 예시입니다. 이 요청을 직접 실행해 보고 싶다면, 명령줄 도구에 붙여넣기 전에 `$BEARER_TOKEN`을 [Bearer Token](/ko/resources/fundamentals/authentication)으로 교체했는지 반드시 확인하세요.

```bash
  curl --request GET \
  --url 'https://api.x.com/2/tweets/search/recent?query=from%3Atwitterdev&tweet.fields=public_metrics&expansions=author_id&user.fields=description' \
  --header 'Authorization: Bearer $BEARER_TOKEN'
```

요청에 이를 지정하면 전달되는 각 게시물에는 다음 필드가 포함되며, 관련 [user object&#39;s](/ko/x-api/fundamentals/data-dictionary#user) 기본 필드와 지정한 필드가 `includes` 내에 함께 반환됩니다. `tweet.author_id`와 `users.id` 필드를 매칭하여 user object를 해당 게시물과 다시 매핑할 수 있습니다.

```
{
  "data": [
    {
      "id": "1263150595717730305",
      "author_id": "2244994945",
      "text": "Do you 👀our new Tweet settings?\n\nWe want to know how and why you’d use a feature like this in the API. Get the details and let us know what you think👇\nhttps://t.co/RtMhhfAcIB https://t.co/8wxeZ9fJER",
      "public_metrics": {
        "retweet_count": 12,
        "reply_count": 13,
        "like_count": 51,
        "quote_count": 7
      }
    }
  ],
  "includes": {
    "users": [
      {
        "id": "2244994945",
        "username": "TwitterDev",
        "description": "#TwitterDev 팀의 공식 채널이자 #TwitterAPI 관련 업데이트, 뉴스 및 이벤트에 대한 공식 소스입니다.",
        "name": "Twitter Dev"
      }
    ]
  }
}
```

특정 하위 필드(예: `public_metrics.retweet_count`)를 개별적으로 요청할 수 없다는 점에 유의하세요. 상위 필드(`public_metrics`)를 지정하면 모든 하위 필드가 반환됩니다. 각 엔드포인트의 API 참조 페이지에 있는 매개변수 테이블에 요청 가능한 모든 필드를 나열해 두었습니다.

전체 필드 목록은 [object model](/ko/x-api/fundamentals/data-dictionary#tweet)에 나와 있습니다. 해당 엔드포인트의 기본 리소스가 아닌 객체에 대해 필드를 확장해 요청하려면, 필드와 함께 [expansions](/ko/x-api/fundamentals/expansions) 매개변수를 사용하세요.