---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="submodules">
  ## 하위 모듈
</div>

* [xdk.posts.client 모듈](/ko/xdks/python/reference/xdk.posts.client)
  * [`PostsClient`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient)
    * [`PostsClient.__init__()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.__init__)
    * [`PostsClient.create()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.create)
    * [`PostsClient.delete()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.delete)
    * [`PostsClient.get_analytics()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_analytics)
    * [`PostsClient.get_by_id()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_by_id)
    * [`PostsClient.get_by_ids()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_by_ids)
    * [`PostsClient.get_counts_all()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_counts_all)
    * [`PostsClient.get_counts_recent()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_counts_recent)
    * [`PostsClient.get_insights28hr()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_insights28hr)
    * [`PostsClient.get_insights_historical()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_insights_historical)
    * [`PostsClient.get_liking_users()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_liking_users)
    * [`PostsClient.get_quoted()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_quoted)
    * [`PostsClient.get_reposted_by()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_reposted_by)
    * [`PostsClient.get_reposts()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_reposts)
    * [`PostsClient.hide_reply()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.hide_reply)
    * [`PostsClient.search_all()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.search_all)
    * [`PostsClient.search_recent()`](/ko/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.search_recent)
* [xdk.posts.models 모듈](/ko/xdks/python/reference/xdk.posts.models)
  * [`CreateRequest`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateRequest)
    * [`CreateRequest.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateRequest.model_config)
  * [`CreateResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateResponse)
    * [`CreateResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateResponse.model_config)
  * [`DeleteResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.DeleteResponse)
    * [`DeleteResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.DeleteResponse.model_config)
  * [`GetAnalyticsResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetAnalyticsResponse)
    * [`GetAnalyticsResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetAnalyticsResponse.model_config)
  * [`GetByIdResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdResponse)
    * [`GetByIdResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdResponse.model_config)
  * [`GetByIdsResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdsResponse)
    * [`GetByIdsResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdsResponse.model_config)
  * [`GetCountsAllResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsAllResponse)
    * [`GetCountsAllResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsAllResponse.model_config)
  * [`GetCountsRecentResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsRecentResponse)
    * [`GetCountsRecentResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsRecentResponse.model_config)
  * [`GetInsights28hrResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsights28hrResponse)
    * [`GetInsights28hrResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsights28hrResponse.model_config)
  * [`GetInsightsHistoricalResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsightsHistoricalResponse)
    * [`GetInsightsHistoricalResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsightsHistoricalResponse.model_config)
  * [`GetLikingUsersResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetLikingUsersResponse)
    * [`GetLikingUsersResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetLikingUsersResponse.model_config)
  * [`GetQuotedResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetQuotedResponse)
    * [`GetQuotedResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetQuotedResponse.model_config)
  * [`GetRepostedByResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostedByResponse)
    * [`GetRepostedByResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostedByResponse.model_config)
  * [`GetRepostsResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostsResponse)
    * [`GetRepostsResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostsResponse.model_config)
  * [`HideReplyRequest`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyRequest)
    * [`HideReplyRequest.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyRequest.model_config)
  * [`HideReplyResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyResponse)
    * [`HideReplyResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyResponse.model_config)
  * [`SearchAllResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchAllResponse)
    * [`SearchAllResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchAllResponse.model_config)
  * [`SearchRecentResponse`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchRecentResponse)
    * [`SearchRecentResponse.model_config`](/ko/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchRecentResponse.model_config)

<div id="module-contents">
  ## 모듈 내용
</div>

이 모듈은 X API의 게시물 엔드포인트에 대한 접근을 제공하며
게시물 관련 모든 기능을 위한 주요 진입점 역할을 합니다.

<div id="class-xdkpostspostsclient">
  ### `class xdk.posts.PostsClient`
</div>

게시물 관련 작업을 위한 Client

#### 매개변수

<ParamField path="path.client" type="Client" />

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client" />

<div id="create">
  ### `create`
</div>

게시물 생성 또는 수정
인증된 사용자에 대해 새로운 게시물을 생성하거나, `edit_options`가 제공된 경우 기존 게시물을 수정합니다.
body: 요청 본문
:returns: 응답 데이터
:rtype: CreateResponse

#### 매개변수

<ParamField path="body.body" type="CreateRequest" />

<div id="delete">
  ### `delete`
</div>

게시물 삭제
인증된 사용자가 소유한 경우, 지정된 ID를 가진 특정 게시물을 삭제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
  삭제할 게시물의 ID입니다.
</ParamField>

#### 반환값

`DeleteResponse` - 응답 데이터

<div id="get_analytics">
  ### `get_analytics`
</div>

게시물 분석 조회
지정된 시간 범위 내 특정 게시물에 대한 분석 데이터를 조회합니다.

#### 매개변수

<ParamField path="path.ids" type="List">
  쉼표로 구분된 게시물 ID 목록입니다. 하나의 요청에는 최대 100개까지 지정할 수 있습니다.
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ 형식입니다. 시간 범위의 종료 시점을 나타내는 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ 형식입니다. 시간 범위의 시작 시점을 나타내는 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.granularity" type="str">
  검색 개수 결과에 사용할 세분화 수준입니다.
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
  표시할 Analytics 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetAnalyticsResponse` - 응답 데이터

<div id="get_by_id">
  ### `get_by_id`
</div>

ID로 게시물 조회
특정 게시물의 ID로 해당 게시물의 세부 정보를 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
  단일 게시물 ID입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetByIdResponse` - 응답 데이터

<div id="get_by_ids">
  ### `get_by_ids`
</div>

ID로 게시물 가져오기
여러 게시물의 ID를 사용하여 게시물의 상세 정보를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.ids" type="List">
  쉼표로 구분한 게시물 ID 목록입니다. 한 번의 요청에서 최대 100개까지 허용됩니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 미디어 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 투표 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 장소 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`GetByIdsResponse` - 응답 데이터

<div id="get_counts_all">
  ### `get_counts_all`
</div>

모든 게시물 개수 가져오기
전체 아카이브에서 검색 쿼리와 일치하는 게시물 개수를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
  게시물을 매칭하기 위한 하나의 쿼리/규칙/필터입니다. 최대 쿼리 길이를 확인하려면 [https://t.co/rulelength](https://t.co/rulelength)를 참조하세요.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물이 제공될 수 있는 가장 이른 UTC 타임스탬프(최근 7일 이내)입니다. 타임스탬프는 초 단위 정밀도를 가지며, 포함형입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물이 제공될 수 있는 가장 최신 UTC 타임스탬프입니다. 타임스탬프는 초 단위 정밀도를 가지며, 배타적입니다(예: 12:00:01은 해당 분의 첫 번째 초를 제외).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  지정된 ID보다 큰(즉, 더 최신) 게시물 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  지정된 ID보다 작은(즉, 더 이전) 게시물 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  이 파라미터는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 파라미터에 사용되는 값은 API가 제공한 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  이 파라미터는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 파라미터에 사용되는 값은 API가 제공한 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  검색 개수 결과에 대한 세분화 수준입니다.
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  표시할 SearchCount 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

최근 게시물 개수 조회
검색 쿼리와 일치하는 지난 7일간의 게시물 개수를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
  게시물을 매칭하기 위한 하나의 query/rule/filter입니다. 최대 query 길이를 확인하려면 [https://t.co/rulelength](https://t.co/rulelength)를 참고하세요.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물이 제공될 기준이 되는 가장 이른 UTC 타임스탬프입니다(최근 7일 이내). 타임스탬프는 초 단위 정밀도를 가지며, 포함 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함합니다).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물이 제공될 기준이 되는 가장 최신 UTC 타임스탬프입니다. 타임스탬프는 초 단위 정밀도를 가지며, 제외 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함하지 않습니다).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  지정된 ID보다 큰(즉, 더 최근의) 게시물 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  지정된 ID보다 작은(즉, 더 이전의) 게시물 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  이 매개변수는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 매개변수에 사용되는 값은 API가 제공한 응답에서 직접 가져온 값 그대로 사용해야 하며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  이 매개변수는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 매개변수에 사용되는 값은 API가 제공한 응답에서 직접 가져온 값 그대로 사용해야 하며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  검색 개수 결과의 집계 단위(세분성)입니다.
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  표시할 SearchCount 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

게시물 28시간 인사이트 가져오기
지정한 게시물에 대해 지난 28시간 동안의 참여 메트릭을 조회합니다.

#### 매개변수

<ParamField path="path.tweet_ids" type="List">
  28시간 메트릭용 게시물 id 목록입니다.
</ParamField>

<ParamField path="path.granularity" type="str">
  메트릭 응답의 세분화 수준입니다.
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  과거 데이터 조회에 대해 요청할 메트릭 목록입니다.
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  표시할 Engagement 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`GetInsights28hrResponse` - 반환 데이터

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

과거 게시물 인사이트 조회
지정된 기간 내 지정한 게시물에 대한 과거 참여 메트릭을 조회합니다.

#### 매개변수

<ParamField path="path.tweet_ids" type="List">
  과거 메트릭을 위한 게시물 id 목록입니다.
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. 시간 범위의 끝을 나타내는 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. 시간 범위의 시작을 나타내는 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.granularity" type="str">
  메트릭 응답의 세분화(granularity) 수준입니다.
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  과거 조회에 대해 요청할 메트릭 목록입니다.
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  표시할 Engagement 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`GetInsightsHistoricalResponse` - 응답 데이터

<div id="get_liking_users">
  ### `get_liking_users`
</div>

좋아요한 사용자 가져오기
특정 게시물의 ID를 사용해 해당 게시물을 좋아요한 사용자 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
  단일 게시물 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  최대 결과 수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  다음 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 사용자 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 트윗 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

인용된 게시물 가져오기
특정 게시물을 ID로 인용한 게시물 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
  단일 게시물 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  반환할 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
  제외할 엔터티 집합입니다(예: ‘replies’ 또는 ‘retweets’).
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

Reposted by 가져오기
특정 게시물 ID를 사용하여 해당 게시물을 리포스트한 사용자 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
  단일 게시물 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  다음 &quot;페이지&quot;의 결과를 가져오는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 게시물 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

Repost 조회
ID로 지정된 특정 게시물을 Repost한 게시물 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
  단일 게시물 ID.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  결과의 최대 개수.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  다음 &#39;페이지&#39;의 결과를 가져오는 데 사용하는 매개변수.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 트윗 필드의 쉼표로 구분된 목록.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 미디어 필드의 쉼표로 구분된 목록.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 투표 필드의 쉼표로 구분된 목록.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 사용자 필드의 쉼표로 구분된 목록.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 장소 필드의 쉼표로 구분된 목록.
</ParamField>

#### 반환값

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

답글 숨기기
인증된 사용자가 소유한 대화의 답글을 숨기거나 다시 표시합니다.

#### 매개변수

<ParamField path="path.tweet_id" type="Any">
  숨기거나 숨김 해제하려는 답글의 ID입니다.
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
  요청 본문
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

모든 게시물 검색
전체 아카이브에서 검색 쿼리와 일치하는 게시물을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
  게시물을 필터링하기 위한 단일 쿼리/규칙/필터입니다. 최대 쿼리 길이는 [https://t.co/rulelength](https://t.co/rulelength)을 참고하세요.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 이 시각부터 게시물이 제공되는 가장 이른 UTC 타임스탬프입니다. 타임스탬프는 초 단위 정밀도를 가지며, 포함 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함합니다).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 이 시각 직전까지 게시물이 제공되는 가장 최신 UTC 타임스탬프입니다. 타임스탬프는 초 단위 정밀도를 가지며, 제외 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 제외합니다).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  지정된 ID보다 큰(즉, 더 최신인) 게시물 ID를 가진 결과만 반환합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  지정된 ID보다 작은(즉, 더 오래된) 게시물 ID를 가진 결과만 반환합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  요청당 반환할 검색 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  다음 ‘페이지’의 결과를 가져오는 데 사용되는 파라미터입니다. 이 파라미터에 사용되는 값은 API 응답에서 직접 가져와야 하며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  다음 ‘페이지’의 결과를 가져오는 데 사용되는 파라미터입니다. 이 파라미터에 사용되는 값은 API 응답에서 직접 가져와야 하며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  결과를 반환할 정렬 순서입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 게시물 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  expansions에 사용할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 미디어 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 투표 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 장소 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

최근 게시물 검색
검색 쿼리와 일치하는 최근 7일 이내의 게시물을 조회합니다.

#### 매개변수

<ParamField path="path.query" type="str">
  게시물을 검색하기 위한 하나의 쿼리/규칙/필터입니다. 최대 쿼리 길이를 확인하려면 [https://t.co/rulelength](https://t.co/rulelength)를 참조하세요.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 이 시각부터 게시물이 제공되기 시작하는 가장 이른 UTC 타임스탬프입니다. 타임스탬프는 초 단위까지 표시되며, 포함 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함합니다).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 이 시각까지 게시물이 제공되는 가장 최신 UTC 타임스탬프입니다. 타임스탬프는 초 단위까지 표시되며, 제외 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 제외합니다).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  지정된 ID보다 큰(즉, 더 최신인) 게시물 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  지정된 ID보다 작은(즉, 더 오래된) 게시물 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  요청당 반환할 수 있는 최대 검색 결과 수입니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  이 매개변수는 다음 &quot;페이지&quot;의 결과를 가져오는 데 사용됩니다. 이 매개변수에 사용하는 값은 API 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  이 매개변수는 다음 &quot;페이지&quot;의 결과를 가져오는 데 사용됩니다. 이 매개변수에 사용하는 값은 API 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  결과를 반환할 정렬 순서입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 미디어 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 투표 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 장소 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`IteratorSearchRecentResponse`