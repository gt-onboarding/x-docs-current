---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

커뮤니티 작업을 위한 Client

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

ID로 Community 조회
지정된 ID를 사용하여 특정 Community의 세부 정보를 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
커뮤니티 id입니다.
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
표시할 커뮤니티 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetByIdResponse` - 응답 데이터

<div id="search">
  ### `search`
</div>

커뮤니티 검색
지정된 검색 쿼리와 일치하는 커뮤니티 목록을 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
커뮤니티를 검색하기 위한 쿼리입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
요청에서 반환할 검색 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
다음 “페이지”의 결과를 가져올 때 사용하는 매개변수입니다. 이 매개변수 값은 API 응답에서 그대로 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 “페이지”의 결과를 가져올 때 사용하는 매개변수입니다. 이 매개변수 값은 API 응답에서 그대로 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
표시할 Community 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorSearchResponse`