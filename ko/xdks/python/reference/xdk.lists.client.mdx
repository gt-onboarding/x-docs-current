---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">상속: object</Badge>

리스트 관련 작업을 처리하기 위한 Client

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

리스트 멤버 추가
지정된 리스트 id를 사용해 해당 리스트에 사용자를 추가합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
멤버를 추가할 List의 id입니다.
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
요청 본문
</ParamField>

<div id="create">
  ### `create`
</div>

리스트 생성
인증된 사용자 계정에 새 리스트를 생성합니다.
body: 요청 본문
:returns: 응답 데이터
:rtype: CreateResponse

#### 매개변수

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

리스트 삭제
인증된 사용자가 소유한 특정 리스트를 해당 id를 사용해 삭제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
삭제할 List의 id 값입니다.
</ParamField>

#### 반환값

`DeleteResponse` - 응답 데이터

<div id="get_by_id">
  ### `get_by_id`
</div>

ID로 List 가져오기
특정 List를 해당 ID로 조회하여 세부 정보를 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
List의 ID입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 List 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`GetByIdResponse` - 응답 데이터

<div id="get_followers">
  ### `get_followers`
</div>

리스트 팔로워 가져오기
특정 리스트의 id를 사용해 해당 리스트를 팔로우하는 사용자 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
리스트의 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

리스트 멤버 가져오기
특정 리스트의 id를 사용해 해당 리스트에 속한 사용자 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
리스트의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
이 매개변수는 지정된 ‘페이지’의 결과를 가져오는 데 사용됩니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

리스트 게시물 가져오기
특정 리스트 ID를 사용해 해당 리스트와 연관된 게시물 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
List의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

리스트 멤버 제거
지정된 리스트의 id와 사용자 id를 사용하여 해당 리스트에서 사용자를 제거합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
멤버를 제거할 List의 id입니다.
</ParamField>

<ParamField path="path.user_id" type="Any">
List에서 제거할 사용자의 id입니다.
</ParamField>

#### 반환값

`RemoveMemberByUserIdResponse` - 응답 데이터

<div id="update">
  ### `update`
</div>

리스트 업데이트
인증된 사용자가 소유한 특정 리스트의 세부 정보를 해당 ID를 사용해 업데이트합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
수정할 리스트의 ID.
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
요청 본문
</ParamField>