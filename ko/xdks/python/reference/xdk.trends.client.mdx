---
title: "TrendsClient"
sidebarTitle: "TrendsClient"
---

<div id="trendsclient">
  ## TrendsClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">상속: object</Badge>

트렌드 관련 작업을 위한 Client

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_ai">
  ### `get_ai`
</div>

ID로 AI 트렌드 가져오기
지정한 ID에 해당하는 AI 트렌드를 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.id" type="Any">
AI 트렌드의 id입니다.
</ParamField>

<ParamField path="path.news_fields" type="List or None" default="None">
표시할 News 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetAiResponse` - 응답 데이터

<div id="get_by_woeid">
  ### `get_by_woeid`
</div>

WOEID로 트렌드 조회
특정 위치를 나타내는 WOEID를 기준으로 해당 위치의 트렌딩 토픽을 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.woeid" type="int">
조회할 트렌드가 속한 장소의 WOEID입니다.
</ParamField>

<ParamField path="path.max_trends" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.trend_fields" type="List or None" default="None">
표시할 트렌드 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetByWoeidResponse` - 응답 데이터

<div id="get_personalized">
  ### `get_personalized`
</div>

개인화된 트렌드를 가져옵니다
인증된 사용자에 대한 개인화된 트렌드 토픽을 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.personalized_trend_fields" type="List or None" default="None">
표시할 PersonalizedTrend 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetPersonalizedResponse` - 응답 데이터