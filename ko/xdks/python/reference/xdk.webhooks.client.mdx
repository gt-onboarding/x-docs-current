---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

웹훅 작업을 위한 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

웹훅 생성
새로운 웹훅 구성을 생성합니다.
body: 요청 본문
:returns: 응답 데이터
:rtype: CreateResponse

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

스트림 링크 만들기
지정된 웹훅으로 FilteredStream 이벤트를 전달하는 링크를 생성합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.webhook_id" type="Any">
FilteredStream 규칙 집합에 연결할 webhook ID입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`CreateStreamLinkResponse` - 응답 데이터

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

웹훅 재생 작업 생성
웹훅으로 전달되었거나 전달을 시도한 모든 이벤트에 대해 최대 24시간 전까지의 이벤트를 재수신하기 위한 재생 작업을 생성합니다.
body: 요청 본문
:returns: 응답 데이터
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

웹훅 삭제
기존 웹훅 설정을 삭제합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.webhook_id" type="Any">
삭제할 webhook의 id입니다.
</ParamField>

#### 반환값

`DeleteResponse` - 응답 데이터

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

스트림 링크 삭제
FilteredStream 이벤트에서 지정된 webhook으로의 링크를 삭제합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.webhook_id" type="Any">
FilteredStream 규칙 집합에 연결할 webhook id입니다.
</ParamField>

#### 반환값

`DeleteStreamLinkResponse` - 응답 데이터

<div id="get">
  ### `get`
</div>

웹훅 조회
클라이언트 앱과 연결된 웹훅 구성 목록을 가져옵니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
표시할 WebhookConfig 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`GetResponse` - 응답 데이터

<div id="get_stream_links">
  ### `get_stream_links`
</div>

스트림 링크 가져오기
필터링 스트림 규칙 집합과 연결된 웹훅 링크 목록을 가져옵니다.
:returns: 응답 데이터
:rtype: GetStreamLinksResponse

#### 반환

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

웹훅 유효성 검사
특정 웹훅에 대해 CRC 검사를 수행합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.webhook_id" type="Any">
확인할 webhook의 ID입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`ValidateResponse` - 응답 데이터