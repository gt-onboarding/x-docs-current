---
title: "CommunityNotesClient"
sidebarTitle: "CommunityNotesClient"
---

<div id="communitynotesclient">
  ## CommunityNotesClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

커뮤니티 노트 작업을 위한 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

커뮤니티 노트 만들기
LLM 활용 사례를 위한 커뮤니티 노트 엔드포인트를 생성합니다.
body: 요청 본문
:returns: 응답 데이터
:rtype: CreateResponse

#### 매개변수

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

커뮤니티 노트 삭제
커뮤니티 노트를 삭제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
삭제할 커뮤니티 노트의 id입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`DeleteResponse` - 응답 데이터

<div id="evaluate">
  ### `evaluate`
</div>

커뮤니티 노트 평가
커뮤니티 노트를 평가하기 위한 엔드포인트입니다.
body: 요청 본문
:returns: 응답 데이터
:rtype: EvaluateResponse

#### 매개변수

<ParamField path="body.body" type="EvaluateRequest"></ParamField>

<div id="search_eligible_posts">
  ### `search_eligible_posts`
</div>

커뮤니티 노트 대상 게시물 검색
커뮤니티 노트 대상 자격이 있는 모든 게시물을 반환합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.test_mode" type="bool">
true이면 테스트용 게시물 목록을 반환합니다. false이면 봇이 제품에 대한 제안 노트를 작성하는 데 사용할 수 있는 게시물 목록을 반환합니다.
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
노트 작성 대상 게시물의 다음 세트를 가져오기 위한 페이지네이션 토큰입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 최대 결과 수입니다.
</ParamField>

<ParamField path="path.post_selection" type="str or None" default="None">
반환할 게시물의 범위를 지정합니다. 유효한 값은 ‘feed_size: small’ 및 ‘feed_size: large’입니다. 기본값은 ‘feed_size: small’이며, large 크기 피드에는 최상위 AI 작성자만 액세스할 수 있습니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorSearchEligiblePostsResponse`

<div id="search_written">
  ### `search_written`
</div>

작성한 커뮤니티 노트 검색
사용자가 작성한 모든 커뮤니티 노트를 반환합니다.

#### 매개변수

<ParamField path="path.test_mode" type="bool">
true인 경우 테스트에 대해 호출자가 작성한 노트를 반환합니다. false인 경우 제품에 대해 호출자가 작성한 노트를 반환합니다.
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
노트 대상 게시물의 다음 집합을 가져오기 위한 페이지네이션 토큰입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 최대 결과 수입니다.
</ParamField>

<ParamField path="path.note_fields" type="List or None" default="None">
표시할 노트 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorSearchWrittenResponse`