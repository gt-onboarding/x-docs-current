---
title: 快速入门
sidebarTitle: 快速入门
keywords: ["屏蔽快速入门", "屏蔽用户快速入门", "屏蔽教程", "屏蔽指南", "屏蔽快速入门指南"]
---

import { Button } from '/snippets/zh/button.mdx';

<div id="getting-started-with-the-blocks-lookup-endpoint">
  ## 开始使用屏蔽查询端点
</div>

本快速入门指南将帮助你使用 [Postman](/zh/tutorials/postman-getting-started) 向屏蔽查询端点发送你的第一个请求。

如果你想查看不同语言的示例代码，请访问我们的 [X API v2 sample code](https://github.com/xdevplatform/Twitter-API-v2-sample-code) GitHub 仓库。

<Note>
  ### 前提条件

  要完成本指南，你需要一组用于对请求进行身份验证的 [密钥和令牌](/zh/resources/fundamentals/authentication)。你可以通过以下步骤生成这些密钥和令牌：

  * [注册开发者账号](https://developer.x.com/en/apply-for-access) 并获得审批。
  * 在开发者门户中创建一个 [项目](/zh/resources/fundamentals/projects) 以及一个关联的 [开发者应用](/zh/resources/fundamentals/developer-apps)。
  * 进入你的 App 的“Keys and tokens”页面以生成所需的凭证。请务必将所有凭证保存在安全的位置。
</Note>

<div id="steps-to-build-a-blocks-lookup-request">
  ### 构建屏蔽查询请求的步骤
</div>

<div id="step-one-start-with-a-tool-or-library">
  #### 步骤一：选择工具或库
</div>

有多种工具、代码示例和库可用于向此端点发出请求，但在这里我们将使用 Postman 工具来简化流程。

要将 X API v2 的 Postman 集合加载到你的环境中，请点击下面的按钮：

<Button href="https://app.getpostman.com/run-collection/9956214-784efcda-ed4c-4491-a4c0-a26470a67400">
  将 X API v2 添加到 Postman
</Button>

在你将 X API v2 集合加载到 Postman 之后，导航到 “Blocks” 文件夹，然后选择 “Blocks Lookup”。
 

<div id="step-two-authenticate-your-request">
  #### 步骤二：为请求进行身份验证
</div>

要正确地向 X API 发起请求，你需要先验证自己是否具备相应的权限。对于此端点，你必须使用 [OAuth 1.0a 用户上下文](/zh/resources/fundamentals/authentication) 或 [OAuth 2.0 Authorization Code with PKCE](resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) 来为请求进行身份验证。

在本示例中，我们将使用 OAuth 1.0a 用户上下文。

你必须将你的密钥和令牌——具体来说是 API Key、API Secret Key、OAuth 1.0a 用户访问令牌，以及 OAuth 1.0a 用户访问令牌密钥——添加到 Postman 中。你可以在 Postman 右上角选择名为 “X API v2” 的环境，然后将你的密钥和令牌填入 &quot;initial value&quot; 和 &quot;current value&quot; 字段中（点击环境下拉菜单旁边的眼睛图标）。

如果你已正确完成上述操作，这些变量会自动被带入请求的 Authorization 选项卡中。
 

<div id="step-three-specify-a-user">
  #### 步骤三：指定一个用户
</div>

对于此 endpoint，你必须指定你的用户 ID，或某个已通过身份验证用户的用户 ID，来查看你屏蔽了哪些人。

在 Postman 中，转到 “Params” 选项卡，在该部分底部的 id 路径变量的 “Value” 列中输入该用户名，确保用户名前后不包含任何空格。
 

|     |     |
| :--- | :--- |
| **Key** | **Value** |
| `id` | （你的用户 ID） |
| max&#95;results | 5   |

<div id="step-four-identify-and-specify-which-fields-you-would-like-to-retrieve">
  #### 步骤四：确定并指定你希望检索的字段
</div>

如果你在完成步骤三后点击 “Send” 按钮发送请求，你将在响应中收到默认的 [user 对象](/zh/x-api/fundamentals/data-dictionary#user) 字段：id、name 和 username。

如果你希望在 id、name 和 username 之外再接收更多字段，你需要在请求中通过 [fields](/zh/x-api/fundamentals/fields) 和/或 [expansions](/zh/x-api/fundamentals/expansions) 参数来指定这些字段。

在本练习中，我们将从不同对象中请求三组额外字段：

1. 主 user 对象中的额外 user.created&#95;at 字段。
2. 返回用户所关联的置顶 Posts 对象的默认字段：id 和 text。
3. 关联 Post 对象中的额外 tweet.created&#95;at 字段。

在 Postman 中，导航到 “Params” 选项卡，在 “Query Params” 表格中添加以下 key:value 对：

|              |                         |                                |
| :----------- | :---------------------- | :----------------------------- |
| **Key**      | **Value**               | **Returned fields**            |
| user.fields  | created&#95;at          | user.created&#95;at            |
| expansions   | pinned&#95;tweet&#95;id | tweet.id, tweet.text           |
| tweet.fields | created&#95;at          | includes.tweets.created&#95;at |

现在你应该会在 “Send” 按钮旁边看到一个类似的 URL，其中包含你自己的用户 ID，而不是 TwitterDev 的用户 ID：

```
      https://api.x.com/2/users/2244994945/blocking?user.fields=created_at&expansions=pinned_tweet_id&tweet.fields=created_at
```

<div id="step-five-make-your-request-and-review-your-response">
  #### 步骤五：发出请求并查看响应
</div>

在完成所有设置后，点击“Send”按钮，你会收到一条与下方示例响应类似的响应：

```
    {
  "data": [
    {
      "created_at": "2008-12-04T18:51:57.000Z",
      "id": "17874544",
      "username": "TwitterSupport",
      "name": "Twitter Support"
    },
    {
      "created_at": "2007-02-20T14:35:54.000Z",
      "id": "783214",
      "username": "Twitter",
      "name": "Twitter"
    },
    {
      "pinned_tweet_id": "1389270063807598594",
      "created_at": "2018-11-21T14:24:58.000Z",
      "id": "1065249714214457345",
      "username": "TwitterSpaces",
      "name": "Spaces"
    },
    {
      "pinned_tweet_id": "1293595870563381249",
      "created_at": "2007-05-23T06:01:13.000Z",
      "id": "6253282",
      "username": "XAPI",
      "name": "X API"
    }
  ],
  "includes": {
    "tweets": [
      {
        "created_at": "2021-05-03T17:26:09.000Z",
        "id": "1389270063807598594",
        "text": "现在,拥有600个或更多关注者的每个人都可以主持一个Space。\n\n根据我们所学到的,这些账户由于其现有受众,可能会有良好的主持体验。在将创建Space的能力带给所有人之前,我们专注于几件事。🧵"
      },
      {
        "created_at": "2020-08-12T17:11:04.000Z",
        "id": "1293595870563381249",
        "text": "X API v2: Early Access released\n\nToday we announced Early Access to the first endpoints of the new Twitter API!\n\n#TwitterAPI #EarlyAccess #VersionBump https://t.co/g7v3aeIbtQ"
      }
    ]
  }
```

<div id="step-six-paginate-through-your-results">
  #### 步骤六：分页浏览结果
</div>

你可能会注意到，在响应的底部有一个 `meta` 对象。如果你收到了一个 `next_token`，则表示还有另一页结果可以获取。要获取下一页结果，你需要取出 `next_token` 字段的值，并将其作为一个额外的 `pagination_token` 参数的值添加到请求中。
 

|     |     |
| :--- | :--- |
| **Key** | **Value** |
| pagination&#95;token | 1D3PU6DRII9HEZZZ |

在添加了这个额外参数后再次发送请求时，由于我们在步骤三中将 `max_results` 指定为 5，接下来的 5 条结果会随着后续的响应载荷一起返回。你可以持续重复这一过程，直到所有结果都被返回；不过，你也可以使用 `max_results` 参数，在每个请求中最多请求 1000 个用户，这样就不需要对结果进行那么多次分页。