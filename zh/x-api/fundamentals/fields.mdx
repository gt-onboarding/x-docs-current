---
title: "字段"
keywords: ["字段", "帖子字段", "用户字段", "字段参数", "API 字段", "数据字段", "自定义响应", "字段选择"]
---

X API v2 的各个 endpoint 配备了一组称为 *fields*（字段）的参数，它们允许你从 endpoint 响应中的每个对象中，只选择你所需要的数据。

默认情况下，Post 对象只返回 `id` 和 `text` 字段，对于 2022 年 9 月 29 日之后创建的帖子，还会返回 `edit_history_tweet_ids` 字段。如果你需要帖子的创建日期或公开指标，就需要使用 `tweet.fields` 参数来请求这些字段。这样可以提供更高程度的自定义，使你能够根据使用场景只请求所需的字段。例如，你可以在请求中包含如下查询字符串：

`?tweet.fields=created_at,public_metrics`

每种对象都有其对应的参数，用于专门请求与该对象关联的字段。以下是当前可用的不同字段参数：

* [Post](/zh/x-api/fundamentals/data-dictionary#tweet) → `tweet.fields`
* [User](/zh/x-api/fundamentals/data-dictionary#user) → `user.fields`
* [Media](/zh/x-api/fundamentals/data-dictionary#media) → `media.fields`
* [Poll](/zh/x-api/fundamentals/data-dictionary#poll) → `poll.fields`
* [Place](/zh/x-api/fundamentals/data-dictionary#place) → `place.fields`

当使用主要返回某一特定对象的 endpoint 时，只需使用与该对象匹配的字段参数，并在该参数的值中以逗号分隔的列表形式指定所需字段，就可以在响应中获取这些字段。

<div id="example">
  ### 示例
</div>

如果你使用 [GET /tweets/search/recent](/zh/x-api/posts/recent-search) 端点，你在该响应中主要会收到 [Tweet objects](/zh/x-api/fundamentals/data-dictionary#tweet)。如果不指定任何 fields 参数，你只会收到默认值 `id` 和 `text`。如果你希望在响应中获取返回的帖子所对应的公开指标，你需要在请求中包含 `tweet.fields` 参数，并将其值设置为 `public_metrics`。

该请求类似如下所示。如果你想直接使用这个请求，请务必将 `$BEARER_TOKEN` 替换为你的 [Bearer Token](/zh/resources/fundamentals/authentication)，并通过命令行工具发送该请求。

```bash
curl --request GET \
  --url 'https://api.x.com/2/tweets/search/recent?query=from%3Atwitterdev&tweet.fields=public_metrics' \
  --header 'Authorization: Bearer $BEARER_TOKEN'
```

如果你在终端中发送该请求，返回的每条帖子都会包含以下字段：

```
{
   "data": {
       "id": "1263150595717730305",
       "public_metrics": {
           "retweet_count": 12,
           "reply_count": 14,
           "like_count": 49,
           "quote_count": 7
       },
       "text": "你看到👀我们新的推文设置了吗？\n\n我们想知道你会如何以及为什么在API中使用这样的功能。获取详细信息并告诉我们你的想法👇\nhttps://t.co/RtMhhfAcIB https://t.co/8wxeZ9fJER"
   }
}
```

如果你想从某个 endpoint 返回的主对象所关联的其他对象中获取一组字段，你需要额外添加一个 expansions 参数。比如，如果你仍然使用之前的同一个 GET search/tweets/recent endpoint，并且希望获取作者的个人资料简介，你需要在请求中同时传递 `expansions=author_id` 和 `user.fields=description`。下面是一个示例。如果你想尝试这个请求，请务必在将其粘贴到命令行工具中之前，将 `$BEARER_TOKEN` 替换为你的 [Bearer Token](/zh/resources/fundamentals/authentication)。

```bash
  curl --request GET \
  --url 'https://api.x.com/2/tweets/search/recent?query=from%3Atwitterdev&tweet.fields=public_metrics&expansions=author_id&user.fields=description' \
  --header 'Authorization: Bearer $BEARER_TOKEN'
```

如果你在请求中指定了这些内容，那么每条返回的帖子都会包含以下字段，且相关[用户对象](/zh/x-api/fundamentals/data-dictionary#user)的默认字段和你指定的字段会在 `includes` 中返回。你可以通过匹配 `tweet.author_id` 和 `users.id` 字段，将用户对象映射回对应的帖子。

```
{
  "data": [
    {
      "id": "1263150595717730305",
      "author_id": "2244994945",
      "text": "你看到👀我们新的推文设置了吗？\n\n我们想知道你会如何以及为什么在API中使用这样的功能。获取详细信息并告诉我们你的想法👇\nhttps://t.co/RtMhhfAcIB https://t.co/8wxeZ9fJER",
      "public_metrics": {
        "retweet_count": 12,
        "reply_count": 13,
        "like_count": 51,
        "quote_count": 7
      }
    }
  ],
  "includes": {
    "users": [
      {
        "id": "2244994945",
        "username": "TwitterDev",
        "description": "The voice of the #TwitterDev team and your official source for updates, news, and events, related to the #TwitterAPI.",
        "name": "Twitter Dev"
      }
    ]
  }
}
```

请注意，你无法请求特定的子字段（例如 `public_metrics.retweet_count`）。当指定顶层字段（`public_metrics`）时，会返回所有子字段。我们已在每个端点的 API 参考页面的参数表中列出了你可以请求的所有字段。

完整字段列表参见[对象模型](/zh/x-api/fundamentals/data-dictionary#tweet)。要为并非该端点主要资源的对象请求扩展及其字段，请将 [expansions](/zh/x-api/fundamentals/expansions) 参数与字段一起使用。