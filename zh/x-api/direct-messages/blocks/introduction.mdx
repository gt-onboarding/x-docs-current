---
title: Introduction
sidebarTitle: Introduction
keywords: ["DM blocks", "direct message blocks", "block DMs", "DM blocking", "direct message blocking", "block messages"]
---

管理 DM 屏蔽的端点使你能够代表已认证用户屏蔽或取消屏蔽指定账号。对于这些端点，有两个可用的 POST 方法：

* **/2/users/:id/dm/block**: 用于屏蔽账号
* **/2/users/:id/dm/unblock**: 用于取消屏蔽账号

<div id="getting-started">
  ### 快速开始
</div>

<div id="authentication">
  ### 身份验证
</div>

由于你是在代表某个用户发起请求，你必须使用 [OAuth 1.0a 用户上下文](/zh/resources/fundamentals/authentication#obtaining-access-tokens-using-3-legged-oauth-flow) 或 [OAuth 2.0 Authorization Code with PKCE](/zh/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) 对这些端点进行身份验证，并使用与该用户关联的用户访问令牌。你可以通过 [三方 OAuth 流程](https://developer.x.com/resources/fundamentals/authentication/obtaining-user-access-tokens)（OAuth 1.0a）或 [Authorization Code with PKCE 授权流程](/zh/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2)（OAuth 2.0）生成该用户访问令牌。

<div id="making-a-request">
  ### 发起请求
</div>

Block

一旦用户在你的应用中完成认证，你就可以代表该用户调用 Block 端点，示例如下：

```
      curl --request POST 'https://api.x.com/2/users/:id/dm/block' --header 'Authorization: ••••••'
```

如果请求成功，你应该会看到如下所示的 JSON 响应：

```
{
   "data": {
       "blocked": true
   }
}
```

**取消屏蔽**

一旦用户通过你的应用完成身份验证，你就可以代表该用户调用 Unblock 端点，如下所示：

```
      curl --request POST 'https://api.x.com/2/users/:id/dm/unblock' --header 'Authorization: ••••••'
```

如果请求成功，您会看到如下所示的 JSON 响应：

```
{
   "data": {
       "blocked": false
   }
}
```
