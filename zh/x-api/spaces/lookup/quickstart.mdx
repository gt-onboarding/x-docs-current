---
title: 快速开始
sidebarTitle: 快速开始
keywords: ["Spaces 查找快速开始", "获取 Spaces 快速开始", "Spaces 查找教程", "Spaces 快速开始指南"]
---

import { Button } from '/snippets/zh/button.mdx';

<div id="getting-started-with-the-spaces-lookup-endpoints">
  ## 开始使用 Spaces 查询端点
</div>

本快速入门指南将帮助你使用 Postman，向其中一个 Spaces 查询端点发出你的第一个请求，并指定一组特定字段。

如果你希望查看使用不同编程语言编写的示例代码，请访问我们的 [X API v2 sample code GitHub](https://github.com/xdevplatform/Twitter-API-v2-sample-code) 仓库。

<Note>
  ### 前提条件

  要完成本指南，你需要拥有一组用于对请求进行认证的 [密钥和令牌](/zh/resources/fundamentals/authentication)。你可以通过以下步骤生成这些密钥和令牌：

  * [注册开发者账号](https://developer.x.com/en/apply-for-access) 并通过审核。
  * 在开发者门户中创建一个 [项目](/zh/resources/fundamentals/projects) 以及一个关联的 [开发者应用](/zh/resources/fundamentals/developer-apps)。
  * 前往你的应用的 “Keys and tokens” 页面生成所需的凭证。请确保将所有凭证保存在安全位置。
</Note>

<div id="steps-to-build-a-spaces-lookup-request">
  #### 构建 Spaces 查询请求的步骤
</div>

*在本示例中，我们将向“按创建者 ID 查询用户 Spaces”的端点发起请求，但你也可以将本快速入门中的方法应用到其他查询请求中。*

**步骤一：选择一个工具或库**

有多种不同的工具、代码示例和库可以用来向此端点发起请求，但这里我们将使用 Postman 工具来简化流程。

要将 X API v2 Postman 集合加载到你的环境中，请点击以下按钮：

<Button href="https://app.getpostman.com/run-collection/9956214-784efcda-ed4c-4491-a4c0-a26470a67400">
  将 X API v2 添加到 Postman
</Button>

在你将 X API v2 集合加载到 Postman 之后，导航到 Spaces 文件夹并找到“Lookup Spaces created by one or more users”请求。
 

**步骤二：为你的请求进行认证**

要正确地向 X API 发起请求，你需要验证自己是否拥有相应的权限。为此，该端点要求你使用 [OAuth 2.0 App-Only](/zh/resources/fundamentals/authentication#app-only-authentication-and-oauth-2-0-bearer-token) 或 [OAuth 2.0 Authorization Code with PKCE](resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) 认证方式对请求进行认证。

为简单起见，我们在本请求中将使用 OAuth 2.0 App-Only，但如果你想要请求私有[指标](/zh/x-api/fundamentals/metrics)或来自私密账号的 Spaces，则需要使用其他认证方式之一。 

要使用 OAuth 2.0 App-Only，你必须将你的密钥和令牌（尤其是 [App Access Token](/zh/resources/fundamentals/authentication#using-and-generating-an-app-only-bearer-token)（也称为 App-only Bearer Token））添加到 Postman。你可以在 Postman 右上角选择名为“X API v2”的环境，然后将你的密钥和令牌填入“initial value”和“current value”字段中（点击环境下拉菜单旁的眼睛图标即可）。

如果你已正确完成这些操作，这些变量将会自动填充到请求的 Authorization 选项卡中。
 

**步骤三：确定并指定你希望从中获取帖子的用户**

你必须在请求中指定你希望获取其实时或即将开始的 Spaces 的用户。在本例中，我们将传入单个用户 ID。

用户 ID 只是用于表示账号 handle 的数值，你可以在该账号的个人资料 URL 中找到它。例如，以下账号的用户名是 XDevelopers。

https://x.com/XDevelopers

要将该用户名转换为用户 ID，你需要使用带有该用户名的 user lookup 端点，并在返回的负载中找到数值型用户 ID。对于 @XDevelopers，用户 ID 为 2244994945。

在 Postman 中，导航到“Params”选项卡，将此用户 ID 输入到 id 参数的“Value”列中。

|     |     |
| :--- | :--- |
| **Key** | **Value** |
| id  | 2244994945 |

**步骤四：确定并指定你希望检索的字段**

如果在完成步骤三后点击“Send”按钮，你将会收到一个 id，这是响应中默认返回的唯一 [Space 对象](/zh/x-api/fundamentals/data-dictionary#space)字段。

如果你希望接收更多字段，就必须在请求中通过 space.fields 或 expansions 参数指定它们。

在本练习中，我们将从不同对象中请求三组附加字段：

* 主 Spaces 对象中的附加 title 字段。
* 指定 creator ID 对应的完整 [user 对象](/zh/x-api/fundamentals/data-dictionary#user)。
* 关联 user 对象中的附加 user.created&#95;at 字段。

在 Postman 中，导航到“Params”选项卡，在“Query Params”表中添加以下 key:value 对：

|     |     |     |
| :--- | :--- | :--- |
| **Key** | **Value** | **Returned fields** |
| space.fields | title | creator&#95;id |
| expansions | creator&#95;id | includes.users.id, includes.users.name, includes.users.username |
| user.fields | created&#95;at | includes.users.created&#95;at |

现在你应该会在“Send”按钮旁边看到如下 URL：

[https://api.x.com/2/spaces/by/creator&#95;ids?user&#95;ids=2244994945&amp;space.fields=creator&#95;id&amp;expansions=creator&#95;id&amp;user.fields=created&#95;at](https://api.x.com/2/spaces/by/creator\\_ids?user\\_ids=2244994945\&space.fields=creator\\_id\&expansions=creator\\_id\&user.fields=created_at)

**步骤五：发出请求并查看响应**

在完成所有设置后，单击“Send”按钮，你就会收到如下响应：

```
{
   "data": [
    {
        "creator_id": "2244994945",
        "id": "1zqKVXPQhvZJB",
        "title": "Hello world 👋",
        "state": "Running"
   },
   "includes": {
       "users": [
           {
               "created_at": "2013-12-14T04:35:55.000Z",
               "name": "Twitter Dev",
               "id": "2244994945",
               "username": "TwitterDev"
           }
       ]
   }
]
}
```
