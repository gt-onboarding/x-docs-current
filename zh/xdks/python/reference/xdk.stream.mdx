---
title: "StreamClient"
sidebarTitle: "StreamClient"
---

<div id="submodules">
  ## 子模块
</div>

* [xdk.stream.client 模块](/zh/xdks/python/reference/xdk.stream.client)
  * [`StreamClient`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient)
    * [`StreamClient.__init__()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.__init__)
    * [`StreamClient.get_rule_counts()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.get_rule_counts)
    * [`StreamClient.get_rules()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.get_rules)
    * [`StreamClient.labels_compliance()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.labels_compliance)
    * [`StreamClient.likes_compliance()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.likes_compliance)
    * [`StreamClient.likes_firehose()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.likes_firehose)
    * [`StreamClient.likes_sample10()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.likes_sample10)
    * [`StreamClient.posts()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts)
    * [`StreamClient.posts_compliance()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_compliance)
    * [`StreamClient.posts_firehose()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose)
    * [`StreamClient.posts_firehose_en()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose_en)
    * [`StreamClient.posts_firehose_ja()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose_ja)
    * [`StreamClient.posts_firehose_ko()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose_ko)
    * [`StreamClient.posts_firehose_pt()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose_pt)
    * [`StreamClient.posts_sample()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_sample)
    * [`StreamClient.posts_sample10()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_sample10)
    * [`StreamClient.update_rules()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.update_rules)
    * [`StreamClient.users_compliance()`](/zh/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.users_compliance)
* [xdk.stream.models 模块](/zh/xdks/python/reference/xdk.stream.models)
  * [`GetRuleCountsResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.GetRuleCountsResponse)
    * [`GetRuleCountsResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.GetRuleCountsResponse.model_config)
  * [`GetRulesResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.GetRulesResponse)
    * [`GetRulesResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.GetRulesResponse.model_config)
  * [`LabelsComplianceResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.LabelsComplianceResponse)
    * [`LabelsComplianceResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.LabelsComplianceResponse.model_config)
  * [`LikesComplianceResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesComplianceResponse)
    * [`LikesComplianceResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesComplianceResponse.model_config)
  * [`LikesFirehoseResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesFirehoseResponse)
    * [`LikesFirehoseResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesFirehoseResponse.model_config)
  * [`LikesSample10Response`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesSample10Response)
    * [`LikesSample10Response.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesSample10Response.model_config)
  * [`PostsComplianceResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsComplianceResponse)
    * [`PostsComplianceResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsComplianceResponse.model_config)
  * [`PostsFirehoseEnResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseEnResponse)
    * [`PostsFirehoseEnResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseEnResponse.model_config)
  * [`PostsFirehoseJaResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseJaResponse)
    * [`PostsFirehoseJaResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseJaResponse.model_config)
  * [`PostsFirehoseKoResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseKoResponse)
    * [`PostsFirehoseKoResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseKoResponse.model_config)
  * [`PostsFirehosePtResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehosePtResponse)
    * [`PostsFirehosePtResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehosePtResponse.model_config)
  * [`PostsFirehoseResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseResponse)
    * [`PostsFirehoseResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseResponse.model_config)
  * [`PostsResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsResponse)
    * [`PostsResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsResponse.model_config)
  * [`PostsSample10Response`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsSample10Response)
    * [`PostsSample10Response.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsSample10Response.model_config)
  * [`PostsSampleResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsSampleResponse)
    * [`PostsSampleResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsSampleResponse.model_config)
  * [`UpdateRulesRequest`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.UpdateRulesRequest)
    * [`UpdateRulesRequest.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.UpdateRulesRequest.model_config)
  * [`UpdateRulesResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.UpdateRulesResponse)
    * [`UpdateRulesResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.UpdateRulesResponse.model_config)
  * [`UsersComplianceResponse`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.UsersComplianceResponse)
    * [`UsersComplianceResponse.model_config`](/zh/xdks/python/reference/xdk.stream.models#xdk.stream.models.UsersComplianceResponse.model_config)

<div id="module-contents">
  ## 模块内容
</div>

此模块提供对 X API 流式端点的访问，
并作为所有流相关功能的主要入口。

<div id="class-xdkstreamstreamclient">
  ### `class xdk.stream.StreamClient`
</div>

用于流式处理操作的 Streaming Client

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client" />

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client" />

<div id="get_rule_counts">
  ### `get_rule_counts`
</div>

获取流规则数量
检索过滤流当前活动规则集中规则的数量。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.rules_count_fields" type="List 或 None" default="None">
  要显示的以逗号分隔的 RulesCount 字段的列表。
</ParamField>

#### 返回

`GetRuleCountsResponse` - 响应数据

<div id="get_rules">
  ### `get_rules`
</div>

获取过滤流规则
获取过滤流当前生效的规则集或其中的子集。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.ids" type="List or None" default="None">
  以逗号分隔的规则 ID 列表。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
  该值通过传入上一请求中返回的、用于结果分页的 `next_token` 来设置。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetRulesResponse`

<div id="labels_compliance">
  ### `labels_compliance`
</div>

帖子标签流（Streaming）
流式传输所有应用到帖子上的标签事件。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回项都代表来自该流的一个数据点。
连接会通过带指数退避的重试逻辑自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回补的时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。将从该最早的 UTC 时间戳开始提供帖子标签。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。将截至该最晚的 UTC 时间戳提供帖子标签。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="likes_compliance">
  ### `likes_compliance`
</div>

流式传输点赞合规数据（Streaming）
为用户流式传输所有与点赞相关的合规数据。
这是一个流式端点，会在数据可用时实时推送数据。
每个返回的项代表流中的一个数据点。
连接由带有指数退避重试逻辑的机制自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回填的时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。开始提供点赞合规事件的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供点赞合规事件的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="likes_firehose">
  ### `likes_firehose`
</div>

流式获取所有点赞（流式）

以实时方式流式获取所有公开的点赞。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回的条目代表流中的一条独立数据记录。
连接会采用指数回退的重试机制自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求补历史数据的时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供点赞数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.like_with_tweet_author_fields" type="List or None" default="None">
  要返回的 LikeWithTweetAuthor 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  要返回的 User 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  要返回的 Tweet 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  用于自定义重试行为、超时和回调的可选 StreamConfig。
</ParamField>

<div id="likes_sample10">
  ### `likes_sample10`
</div>

采样点赞流（Streaming）
以实时方式流式传输 10% 的公开点赞样本。
这是一个流式端点，会在数据可用时实时输出数据。
每个返回的条目对应流中的一个独立数据点。
连接采用带指数退避的重试逻辑自动管理。
如果流断开，SDK 会自动重新连接，无需客户端干预。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回补的时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供点赞数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.like_with_tweet_author_fields" type="List or None" default="None">
  要返回的 LikeWithTweetAuthor 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  要返回的 User 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  要返回的 Tweet 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="posts">
  ### `posts`
</div>

流式传输已过滤的帖子（Streaming）
实时流式传输与当前规则集匹配的帖子。
这是一个流式端点，会在数据可用时实时输出数据。
每个返回的项代表流中的一个独立数据点。
连接由带指数退避的重试逻辑自动管理。
如果流中断，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回填的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。开始提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  要显示的帖子字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  要显示的媒体字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  要显示的投票字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  要显示的地点字段的逗号分隔列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="posts_compliance">
  ### `posts_compliance`
</div>

流式获取帖子合规数据（Streaming）
持续流式输出与帖子相关的所有合规数据。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回的条目代表流中的一个数据点。
连接由带有指数退避重试逻辑的机制自动管理。
如果流断开连接，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回填的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。开始提供帖子合规事件的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子合规事件截至的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_firehose">
  ### `posts_firehose`
</div>

流式获取所有帖子（Streaming）
实时流式传输所有公开帖子。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出的条目代表流中的一个独立数据点。
连接会通过指数退避重试逻辑自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回补的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的帖子字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的需扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的地点字段列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_firehose_en">
  ### `posts_firehose_en`
</div>

流式获取英文帖子（Streaming）
实时流式传输所有公开的英文帖子。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出项代表流中的一个数据点。
连接由指数退避重试逻辑自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回补的数据时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  要显示的帖子字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  要显示的媒体字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  要显示的投票字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  要显示的地点字段的逗号分隔列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="posts_firehose_ja">
  ### `posts_firehose_ja`
</div>

流式获取日语帖子（Streaming）
实时流式传输所有公开的日语帖子。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回项表示来自该流的单个数据点。
连接由指数退避重试逻辑自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回补的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的要显示的帖子字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的要显示的地点字段列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试策略、超时时间和回调。
</ParamField>

<div id="posts_firehose_ko">
  ### `posts_firehose_ko`
</div>

流式获取韩语帖子（流式传输）
实时流式获取所有公开的韩语帖子。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出的条目代表该流中的一个独立数据点。
连接由带有指数退避的重试逻辑自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回补的时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的帖子字段列表，用于展示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的媒体字段列表，用于展示。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的投票字段列表，用于展示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的用户字段列表，用于展示。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的地点字段列表，用于展示。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_firehose_pt">
  ### `posts_firehose_pt`
</div>

葡萄牙语帖子流（Streaming）
以实时方式传输所有公开的葡萄牙语帖子。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回的项代表流中的一个独立数据点。
连接采用带指数退避的重试逻辑进行自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回填数据的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。开始提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。结束提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的帖子字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的媒体字段列表，用于显示。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的投票字段列表，用于显示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的用户字段列表，用于显示。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的地点字段列表，用于显示。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_sample">
  ### `posts_sample`
</div>

流式获取抽样帖子（Streaming）
以实时方式流式获取 1% 的公开帖子样本。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回的条目代表来自该流的一个独立数据点。
连接由带指数退避的重试逻辑自动管理。
如果流连接断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回溯的分钟数。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的帖子字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的媒体字段列表，用于显示。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的投票字段列表，用于显示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的用户字段列表，用于显示。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的地点字段列表，用于显示。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_sample10">
  ### `posts_sample10`
</div>

以流式方式传输 10% 抽样的帖子（Streaming）
实时传输 10% 抽样的公开帖子。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出项代表来自该流的一个独立数据点。
连接由带指数退避的重试逻辑自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求回填的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  格式为 YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  格式为 YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的要显示的帖子字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的要显示的地点字段列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="update_rules">
  ### `update_rules`
</div>

更新流规则
在过滤流的当前生效规则集中添加或删除规则。

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="UpdateRulesRequest">
  请求体
</ParamField>

<div id="users_compliance">
  ### `users_compliance`
</div>

流式接收 Users 合规数据（Streaming）
流式传输与 Users 相关的所有合规数据。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出的元素表示流中的一个数据点。
连接将通过指数退避重试逻辑自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
  分区号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  请求的回填时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。开始提供用户合规事件的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。停止提供用户合规事件的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  用于自定义重试行为、超时和回调的可选 StreamConfig。
</ParamField>