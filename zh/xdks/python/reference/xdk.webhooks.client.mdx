---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于处理 Webhook 操作的 Client

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

创建 webhook
创建一个新的 webhook 配置。
body: 请求体
:returns: 响应数据
:rtype: CreateResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

创建流链接
创建一个链接，用于将 FilteredStream 事件发送到指定的 webhook。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要关联到你的 FilteredStream 规则集的 webhook ID。
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
要展示的帖子字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
要展示的媒体字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
要展示的投票字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
要展示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
要展示的地点字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`CreateStreamLinkResponse` - 响应数据

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

为 webhook 创建回放作业
创建一个回放作业，用于检索在过去最多 24 小时内所有已投递或尝试投递到该 webhook 的事件。
body：请求体
:returns: 响应数据
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

删除 webhook
删除一个现有的 webhook 配置。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
待删除 webhook 的 id。
</ParamField>

#### 返回

`DeleteResponse` - 响应数据

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

删除流链接
从 FilteredStream 事件中移除指向指定 webhook 的链接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要与您的 FilteredStream 规则集关联的 webhook id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteStreamLinkResponse` - 响应数据

<div id="get">
  ### `get`
</div>

获取 webhook
获取与某客户端应用关联的 webhook 配置列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
以逗号分隔的 WebhookConfig 字段列表，用于指定要显示的字段。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetResponse` - 响应数据

<div id="get_stream_links">
  ### `get_stream_links`
</div>

获取流链接
获取与某个过滤流规则集关联的 webhook 链接列表。
:returns: 响应数据
:rtype: GetStreamLinksResponse

#### 返回

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

验证 webhook
为指定的 webhook 触发 CRC 校验。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要检查的 webhook 的 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`ValidateResponse`：响应数据