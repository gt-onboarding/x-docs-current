---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于执行 Communities 相关操作的 Client

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

按 ID 获取 Community
检索指定 ID 的特定 Community 的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
Community 的 id。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
要显示的以逗号分隔的 Community 字段列表。
</ParamField>

<div id="returns">
  #### 返回
</div>

`GetByIdResponse` - 响应数据

<div id="search">
  ### `search`
</div>

搜索 Communities
获取与指定搜索查询匹配的 Communities 列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.query" type="str">
用于搜索社区的查询条件。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
单个请求可返回的最大搜索结果数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。该参数所使用的值直接来自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。该参数所使用的值直接来自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
要显示的社区字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回
</div>

`IteratorSearchResponse`