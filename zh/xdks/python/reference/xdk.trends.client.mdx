---
title: "TrendsClient"
sidebarTitle: "TrendsClient"
---

<div id="trendsclient">
  ## TrendsClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于趋势相关操作的 Client

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_ai">
  ### `get_ai`
</div>

通过 ID 获取 AI 趋势
检索指定 ID 的 AI 趋势。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
AI 趋势的 id。
</ParamField>

<ParamField path="path.news_fields" type="List or None" default="None">
要显示的新闻字段，以逗号分隔。
</ParamField>

#### 返回

`GetAiResponse` - 响应数据

<div id="get_by_woeid">
  ### `get_by_woeid`
</div>

通过 WOEID 获取趋势
检索由其 WOEID 指定的特定位置的热门话题。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.woeid" type="int">
要查询趋势的地点的 WOEID。
</ParamField>

<ParamField path="path.max_trends" type="int or None" default="None">
结果数量上限。
</ParamField>

<ParamField path="path.trend_fields" type="List or None" default="None">
要显示的 Trend 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByWoeidResponse` - 响应数据

<div id="get_personalized">
  ### `get_personalized`
</div>

获取个性化趋势
为已通过身份验证的用户获取个性化趋势话题。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.personalized_trend_fields" type="List or None" default="None">
以逗号分隔的 PersonalizedTrend 字段列表，用于指定要显示的内容。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetPersonalizedResponse` - 响应数据