---
title: 快速入门
sidebarTitle: 快速入门
---

本示例展示了如何使用 XDK，并通过 Bearer Token 进行认证来快速搜索帖子。

<div id="step-1-install-the-sdk">
  ## 第 1 步：安装 SDK
</div>

```bash
pip install xdk
```

<div id="step-2-get-your-bearer-token">
  ## 步骤 2：获取 Bearer Token
</div>

1. 登录 [X Developer Portal](https://developer.x.com/en/portal/dashboard)。
2. 创建一个应用或选择现有应用。
3. 在“Keys and Tokens”下生成一个 Bearer Token（仅应用级认证）。

<div id="step-3-write-and-run-your-first-script">
  ## 步骤 3：编写并运行你的第一个脚本
</div>

新建一个名为 `quickstart.py` 的文件：

```python
# 导入 client
from xdk import Client
# 将此处替换为您的实际 Bearer Token
client = Client(bearer_token="YOUR_BEARER_TOKEN_HERE")
# 获取提及 "api" 的近期帖子
# search_recent 返回一个迭代器,因此需要对其进行迭代
for page in client.posts.search_recent(query="api", max_results=10):
    if page.data and len(page.data) > 0:
        # 访问第一个帖子 - Pydantic 模型同时支持属性访问和字典访问
        first_post = page.data[0]
        post_text = first_post.text if hasattr(first_post, 'text') else first_post.get('text', '')
        print(f"Latest Post: {post_text}")
        break
    else:
        print("未找到帖子。")
        break
```

运行：

```bash
python quickstart.py
```

**预期输出**:

```
Latest Post: XDK Python SDK 的重要更新！
```

**故障排查**：如果遇到 401 错误，请仔细检查你的 Bearer Token 是否正确。对于速率限制错误（429），请等待一段时间后再重试。

<div id="next-steps">
  ## 后续步骤
</div>

* 查看[身份验证](/zh/xdks/python/authentication)，了解如何使用 Bearer Token（应用仅模式）认证、带 PKCE 的 OAuth 2.0（用户上下文）以及 OAuth 1.0a（传统用户上下文）。
* 了解[分页](/zh/xdks/python/pagination)，以便在需要返回大量结果又不想处理多次 API 调用的场景中使用。
* 深入学习[流式处理](/zh/xdks/python/streaming)，了解如何处理实时数据。
  要获取使用 Python XDK 的详细代码示例，请查看我们的[代码示例 GitHub 仓库](https://github.com/xdevplatform/samples/tree/main/python)。