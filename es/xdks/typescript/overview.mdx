---
title: "XDK de TypeScript"
sidebarTitle: "Descripción general"
---

Un SDK completo de TypeScript para la X API (antes conocida como Twitter API) con funciones avanzadas como paginación inteligente, varios métodos de autenticación, streaming en tiempo real y tipado completamente seguro.

<div id="key-features">
  ## Funcionalidades clave
</div>

* **🔐 Autenticación**: Contexto de usuario (OAuth1.0a, OAuth2.0) y autenticación solo para la Aplicación (Bearer Token)
* **🔄 Paginación**: Paginación automática compatible con iteración asíncrona
* **📡 Streaming**: Streaming basado en eventos con reconexión automática
* **📚 Seguridad de tipos**: Definiciones completas de TypeScript para todos los endpoints y parámetros
* **🎯 Compatibilidad total con X API**: Usuarios, Publicaciones, Listas, Marcadores, Comunidades y más

<div id="quick-start">
  ## Inicio rápido
</div>

<CodeGroup dropdown>
  ```typescript quickstart.ts theme={null}
  import { 
      Client, 
      type ClientConfig,
      type Users
  } from '@xdevplatform/xdk';

  const config: ClientConfig = { bearerToken: 'your-bearer-token' };

  const client: Client = new Client(config);

  async function main(): Promise<void> {
    const userResponse: Users.GetByUsernameResponse = await client.users.getByUsername('XDevelopers');
    const username: string = userResponse.data?.username!;
    console.log(username);
  }

  main();
  ```

  ```javascript quickstart.js theme={null}
  import { Client } from '@xdevplatform/xdk';

  const client = new Client({ bearerToken: 'your-bearer-token' });

  const userResponse = await client.users.getByUsername('XDevelopers');
  const username = userResponse.data.username;
  console.log(username);
  ```
</CodeGroup>

<Info>
  Para ver ejemplos de código detallados que utilizan el XDK de JavaScript/TypeScript, consulta nuestro [repositorio de ejemplos de código en GitHub](https://github.com/xdevplatform/samples/tree/main/javascript).
</Info>

<div id="whats-next">
  ## ¿Qué sigue?
</div>

* [Guía de instalación](/es/xdks/typescript/install) - Configura el SDK en tu proyecto
* [Autenticación](/es/xdks/typescript/authentication) - Conoce los distintos métodos de autenticación
* [Paginación](/es/xdks/typescript/pagination) - Aprende cómo funciona la paginación de datos
* [Streaming](/es/xdks/typescript/streaming) - Aprende sobre el streaming de datos en tiempo real
* [Referencia de la API](/es/xdks/typescript/reference/Client) - Lee la documentación completa de la API