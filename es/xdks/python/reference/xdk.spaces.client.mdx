---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones de Spaces

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Obtiene los compradores de entradas de un Space
Recupera una lista de usuarios que compraron entradas para un Space específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El ID del Espacio que se va a obtener.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una ‘página’ específica de resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de publicación para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

Obtiene Spaces por IDs de creador
Obtiene detalles de los Spaces creados por las IDs de usuario especificadas.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.user_ids" type="List">
Los id de usuarios en los que buscar.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista separada por comas de campos de Space para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista separada por comas de campos de tema para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByCreatorIdsResponse` - Datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener un Space por ID
Obtiene los detalles de un Space específico por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El ID del Space que se va a recuperar.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista de campos de Space, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se van a expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista de campos de tema, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdResponse` - Datos de la respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener Spaces por id
Recupera los detalles de varios Spaces a partir de sus id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
La lista de IDs de Space que se devolverán.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista separada por comas de campos de Space para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User para mostrar.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista separada por comas de campos de Topic para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdsResponse` - datos de la respuesta

<div id="get_posts">
  ### `get_posts`
</div>

Obtener publicaciones de un Space
Recupera una lista de publicaciones compartidas en un Space específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El ID del Space que se va a recuperar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número de Publicaciones que se obtendrán del Space proporcionado. Si no se especifica, el valor se establecerá en el máximo de 100.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia que se van a mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de la encuesta que se van a mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos del usuario que se van a mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos del lugar que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetPostsResponse` - datos de la respuesta

<div id="search">
  ### `search`
</div>

Buscar Spaces
Obtiene una lista de Spaces que coinciden con la consulta de búsqueda especificada.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
La consulta de búsqueda.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
El estado de los Spaces que se deben buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número de resultados que se devolverán.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista separada por comas de campos de Space que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista separada por comas de campos de tema que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`SearchResponse` - Datos de respuesta