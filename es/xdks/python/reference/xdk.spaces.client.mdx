---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones con Spaces

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Obtener compradores de entradas de un Space
Recupera una lista de Usuarios que compraron entradas para un Space específico identificado por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El ID del Space que se va a obtener.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Lista de campos de usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Lista de campos de Publicación, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Valor de retorno
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

Obtener Spaces por id de creadores
Recupera los detalles de los Spaces creados por las id de usuario especificadas.

#### Parameters

<ParamField path="path.user_ids" type="List">
Los IDs de usuarios en los que realizar la búsqueda.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista separada por comas de campos de Space para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se deben expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista separada por comas de campos de tema para mostrar.
</ParamField>

#### Devuelve

`GetByCreatorIdsResponse` - Datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener un Space por ID
Obtiene los detalles de un Space específico por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El ID del Space que se va a obtener.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista de campos de Space, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista de campos de Topic, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`GetByIdResponse` - Datos de respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtiene Spaces por ids
Obtiene los detalles de varios Spaces por sus ids.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
La lista de IDs de Spaces que se van a devolver.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista de campos de Space, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista de campos de Topic, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`GetByIdsResponse` - Datos de respuesta

<div id="get_posts">
  ### `get_posts`
</div>

Obtener Publicaciones de un Space
Devuelve una lista de Publicaciones compartidas en un Space específico identificado por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El ID del Space que se va a obtener.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número de Publicaciones que se obtendrán del Space indicado. Si no se especifica, el valor tomará el máximo de 100.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugares que se mostrarán.
</ParamField>

#### Devuelve

`GetPostsResponse` - datos de la respuesta

<div id="search">
  ### `search`
</div>

Buscar Spaces
Obtiene una lista de Spaces que coinciden con la consulta de búsqueda especificada.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
La consulta de búsqueda.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
El estado de los Spaces que se va a buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número de resultados que se devolverán.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista separada por comas de campos de Space que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se ampliarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista separada por comas de campos de tema que se mostrarán.
</ParamField>

#### Devuelve

`SearchResponse` - Datos de la respuesta