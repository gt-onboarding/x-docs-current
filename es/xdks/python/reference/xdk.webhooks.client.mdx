---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: objeto</Badge>

Client para operaciones de webhooks

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Crear un webhook
Crea una nueva configuración de webhook.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

Crear enlace de streaming
Crea un enlace para enviar eventos de FilteredStream al webhook indicado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID del webhook que se vinculará a tu conjunto de reglas de FilteredStream.
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
Una lista de campos de Publicación separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
Una lista de campos separados por comas para expandir.
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
Una lista de campos de contenido multimedia separados por comas para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
Una lista de campos de encuestas separados por comas para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
Una lista de campos de usuario separados por comas para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
Una lista de campos de lugares separados por comas para mostrar.
</ParamField>

<div id="returns">
  #### Valores devueltos
</div>

`CreateStreamLinkResponse` - Datos de la respuesta

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

Crear un trabajo de repetición para el webhook
Crea un trabajo de repetición para recuperar eventos de hasta las últimas 24 horas correspondientes a todos los eventos que se hayan entregado o se haya intentado entregar al webhook.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Eliminar un webhook
Elimina la configuración de un webhook existente.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook que se va a eliminar.
</ParamField>

#### Devuelve

`DeleteResponse` - Datos de la respuesta

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

Eliminar enlace de stream
Elimina el enlace entre los eventos de FilteredStream y el webhook especificado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook que se enlazará a tu conjunto de reglas de FilteredStream.
</ParamField>

#### Devuelve

`DeleteStreamLinkResponse` - Datos de la respuesta

<div id="get">
  ### `get`
</div>

Obtener webhook
Obtener una lista de configuraciones de webhooks asociadas a una aplicación cliente.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
Una lista separada por comas de campos de WebhookConfig que se mostrarán.
</ParamField>

#### Devuelve

`GetResponse` - Datos de la respuesta

<div id="get_stream_links">
  ### `get_stream_links`
</div>

Obtener enlaces del stream
Obtiene una lista de enlaces de webhook asociados con un conjunto de reglas de filtered stream.
:returns: datos de la respuesta
:rtype: GetStreamLinksResponse

#### Devuelve

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

Validar webhook
Ejecuta una comprobación CRC para un webhook determinado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
La id del webhook que se va a verificar.
</ParamField>

#### Devuelve

`ValidateResponse` - Datos de la respuesta