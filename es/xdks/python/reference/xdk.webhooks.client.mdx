---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones de webhooks

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Crear un webhook
Crea una nueva configuración de un webhook.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

Crear enlace de streaming
Crea un enlace para enviar eventos de FilteredStream al webhook especificado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID de webhook que se va a vincular a tu conjunto de reglas de FilteredStream.
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
Una lista de campos de publicación separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
Una lista de campos separados por comas para expandir.
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
Una lista de campos de contenido multimedia separados por comas para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
Una lista de campos de encuesta separados por comas para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
Una lista de campos de usuario separados por comas para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
Una lista de campos de lugar separados por comas para mostrar.
</ParamField>

#### Devuelve

`CreateStreamLinkResponse` - Datos de la respuesta

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

Crear trabajo de reproducción para un webhook
Crea un trabajo de reproducción para recuperar eventos de hasta las últimas 24 horas correspondientes a todos los eventos entregados o cuya entrega se haya intentado al webhook.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Eliminar un webhook
Elimina una configuración de webhook existente.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook que se va a eliminar.
</ParamField>

#### Devuelve

`DeleteResponse` - Datos de la respuesta

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

Eliminar enlace de stream
Elimina el enlace de eventos de FilteredStream al webhook indicado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook que se vinculará a tu conjunto de reglas de FilteredStream.
</ParamField>

<div id="returns">
  #### Valor devuelto
</div>

`DeleteStreamLinkResponse` - Datos de respuesta

<div id="get">
  ### `get`
</div>

Obtener webhook
Obtén una lista de configuraciones de webhooks asociadas con una aplicación cliente.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
Una lista de campos WebhookConfig, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`GetResponse` - Datos de la respuesta

<div id="get_stream_links">
  ### `get_stream_links`
</div>

Obtiene enlaces de flujo
Obtiene una lista de enlaces de webhook asociados con un conjunto de reglas de flujo filtrado.
:returns: Datos de la respuesta
:rtype: GetStreamLinksResponse

#### Devuelve

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

Validar webhook
Inicia una comprobación CRC para un webhook determinado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook que se va a comprobar.
</ParamField>

#### Devuelve

`ValidateResponse` - Datos de respuesta