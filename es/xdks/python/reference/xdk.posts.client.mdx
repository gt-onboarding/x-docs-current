---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="postsclient">
  ## PostsClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones con publicaciones

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Crear o editar una Publicación
Crea una nueva Publicación para el usuario autenticado o edita una Publicación existente cuando se proporcionan las edit_options.
body: Cuerpo de la solicitud
:returns: datos de la respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Eliminar Publicación
Elimina una Publicación específica por su id, si pertenece al usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la Publicación que se eliminará.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteResponse` - datos de la respuesta

<div id="get_analytics">
  ### `get_analytics`
</div>

Obtener analíticas de Publicaciones
Recupera datos analíticos de las Publicaciones especificadas en un intervalo de tiempo definido.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
Una lista de ids de Publicación separados por comas. Se permiten hasta 100 en una sola solicitud.
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC que representa el final del intervalo de tiempo.
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC que representa el inicio del intervalo de tiempo.
</ParamField>

<ParamField path="path.granularity" type="str">
La granularidad de los resultados de conteos de búsqueda.
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
Una lista separada por comas de campos de Analytics que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetAnalyticsResponse` - Datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener Publicación por id
Obtiene los detalles de una Publicación específica a partir de su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
Un único id de Publicación.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de contenido multimedia, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista de campos de encuesta, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista de campos de lugar, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdResponse` - Datos de la respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener Publicaciones por id
Obtiene los detalles de varias Publicaciones por sus id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
Una lista separada por comas de IDs de Publicación. Se permiten hasta 100 en una sola solicitud.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdsResponse` - Datos de respuesta

<div id="get_counts_all">
  ### `get_counts_all`
</div>

Obtener el recuento total de Publicaciones
Devuelve el número de Publicaciones que coinciden con una consulta de búsqueda en todo el archivo.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
Una consulta/regla/filtro para coincidir con Publicaciones. Revisa [https://t.co/rulelength](https://t.co/rulelength) para identificar la longitud máxima de la consulta.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua (de los últimos 7 días) desde la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es inclusiva (es decir, 12:00:01 incluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es exclusiva (es decir, 12:00:01 excluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
Devuelve resultados con un id de Publicación mayor (es decir, más reciente) que el id especificado.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
Devuelve resultados con un id de Publicación menor (es decir, más antigua) que el id especificado.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente ‘página’ de resultados. El valor utilizado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no se debe modificar.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente ‘página’ de resultados. El valor utilizado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no se debe modificar.
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
La granularidad para los resultados de conteos de búsqueda.
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
Una lista de campos SearchCount, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

Obtener la cantidad de Publicaciones recientes
Obtiene la cantidad de Publicaciones de los últimos 7 días que coinciden con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
Una consulta/regla/filtro para hacer coincidir Publicaciones. Consulta [https://t.co/rulelength](https://t.co/rulelength) para identificar la longitud máxima de la consulta.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua (de los 7 días más recientes) a partir de la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es inclusiva (es decir, 12:00:01 incluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es exclusiva (es decir, 12:00:01 excluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
Devuelve resultados con un ID de Publicación mayor (es decir, más reciente) que el ID especificado.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
Devuelve resultados con un ID de Publicación menor (es decir, más antiguo) que el ID especificado.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente “página” de resultados. El valor usado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente “página” de resultados. El valor usado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
La granularidad de los resultados de conteos de búsqueda.
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
Una lista separada por comas de campos de SearchCount que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

Obtén insights de Publicaciones de las últimas 28 horas
Recupera las métricas de interacción de Publicaciones específicas de las últimas 28 horas.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.tweet_ids" type="List">
Lista de PostIds para métricas de 28 horas.
</ParamField>

<ParamField path="path.granularity" type="str">
Granularidad de las métricas en la respuesta.
</ParamField>

<ParamField path="path.requested_metrics" type="List">
Métricas solicitadas para una consulta histórica.
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
Una lista separada por comas de campos de Engagement que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetInsights28hrResponse` - datos de la respuesta

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

Obtener insights históricos de las Publicaciones
Recupera métricas de interacción históricas para las Publicaciones especificadas en un intervalo de tiempo determinado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.tweet_ids" type="List">
Lista de PostIds para métricas históricas.
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC que representa el final del intervalo de tiempo.
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC que representa el inicio del intervalo de tiempo.
</ParamField>

<ParamField path="path.granularity" type="str">
Granularidad de la respuesta de métricas.
</ParamField>

<ParamField path="path.requested_metrics" type="List">
Métricas a solicitar para la consulta histórica.
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
Una lista de campos de interacción, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetInsightsHistoricalResponse` - Datos de la respuesta

<div id="get_liking_users">
  ### `get_liking_users`
</div>

Obtener usuarios que dieron "Me gusta"
Recupera una lista de usuarios que dieron "Me gusta" a una Publicación específica, identificada por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
Un único id de Publicación.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

Obtener Publicaciones citadas
Obtiene una lista de Publicaciones que citan una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de una única Publicación.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados que se devolverán.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se excluirán (por ejemplo, “replies” o “retweets”).
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se ampliarán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

Obtener usuarios que la han republicado
Recupera una lista de usuarios que han republicado una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
Un único id de Publicación.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se deben expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación separados por comas para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

Obtener Reposts
Devuelve una lista de Publicaciones que han hecho Repost de una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
Un único id de una Publicación.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de Encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de Lugar para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

Ocultar respuesta
Oculta o deja de ocultar una respuesta en una conversación que pertenece al usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.tweet_id" type="Any">
El id de la respuesta que deseas ocultar o dejar de ocultar.
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
Cuerpo de la solicitud
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

Buscar todas las Publicaciones
Recupera Publicaciones del archivo completo que coincidan con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
Una consulta/regla/filtro para buscar Publicaciones coincidentes. Consulta [https://t.co/rulelength](https://t.co/rulelength) para identificar la longitud máxima de la consulta.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es inclusiva (es decir, 12:00:01 incluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es exclusiva (es decir, 12:00:01 excluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
Devuelve resultados con un id de Publicación mayor que (es decir, más reciente que) el id especificado.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
Devuelve resultados con un id de Publicación menor que (es decir, más antiguo que) el id especificado.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados de búsqueda que se devolverán en una solicitud.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente "página" de resultados. El valor usado con el parámetro se obtiene directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente "página" de resultados. El valor usado con el parámetro se obtiene directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
El orden en el que se devolverán los resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

Buscar Publicaciones recientes
Obtiene Publicaciones de los últimos 7 días que coincidan con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
Una consulta/regla/filtro para hacer coincidir Publicaciones. Consulta <a href="https://t.co/rulelength">https://t.co/rulelength</a> para identificar la longitud máxima de la consulta.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se proporcionarán las Publicaciones. Esta marca de tiempo tiene granularidad de segundos y es inclusiva (es decir, 12:00:01 incluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. Esta marca de tiempo tiene granularidad de segundos y es exclusiva (es decir, 12:00:01 excluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
Devuelve resultados con un id de Publicación mayor (es decir, más reciente) que el id especificado.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
Devuelve resultados con un id de Publicación menor (es decir, más antigua) que el id especificado.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados de búsqueda que se devolverán en una solicitud.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados. El valor utilizado con el parámetro se toma directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados. El valor utilizado con el parámetro se toma directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
El orden en el que se deben devolver los resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorSearchRecentResponse`