---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: objeto</Badge>

Cliente para operaciones con usuarios

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

Bloquear MD
Bloquea los mensajes directos enviados por o dirigidos a un usuario específico mediante su id para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del User de destino para el que el usuario autenticado solicita bloquear los DMs.
</ParamField>

#### Devuelve

`BlockDmsResponse` - Datos de la respuesta

<div id="create_bookmark">
  ### `create_bookmark`
</div>

Crear marcador
Agrega una Publicación a los marcadores del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del User de origen autenticado para el cual se añadirán marcadores.
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
Cuerpo de la solicitud
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

Eliminar marcador
Elimina una Publicación de los marcadores del usuario autenticado usando su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado cuyo marcador se eliminará.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
El ID de la Publicación que el Usuario de origen eliminará de sus marcadores.
</ParamField>

#### Devuelve

`DeleteBookmarkResponse` - Datos de la respuesta

<div id="follow_list">
  ### `follow_list`
</div>

Seguir lista
Hace que el usuario autenticado comience a seguir una Lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que seguirá la Lista.
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
Cuerpo de la solicitud
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

Seguir a un usuario
Hace que el usuario autenticado siga a un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que solicita seguir al usuario de destino.
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
Cuerpo de la solicitud
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

Obtener bloqueos
Obtiene una lista de usuarios bloqueados por la id de usuario especificada.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de Usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

Obtener carpetas de Marcadores
Obtiene una lista de carpetas de Marcadores creadas por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<div id="returns">
  #### Valores devueltos
</div>

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

Obtener marcadores
Obtiene una lista de Publicaciones guardadas como marcadores por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

Obtener marcadores por id de carpeta
Obtiene las Publicaciones de una carpeta de marcadores específica por su id para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.folder_id" type="Any">
El id de la carpeta de marcadores para la que el Usuario autenticado intenta recuperar Publicaciones.
</ParamField>

#### Devuelve

`GetBookmarksByFolderIdResponse` - Datos de respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener usuario por id
Recupera los detalles de un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a buscar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de publicación para mostrar.
</ParamField>

#### Devuelve

`GetByIdResponse` - Datos de respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener usuarios por id
Obtiene los detalles de varios usuarios a partir de sus id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
Una lista de ID de usuario separados por comas. Puedes especificar hasta 100 ID.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación para mostrar.
</ParamField>

#### Devuelve

`GetByIdsResponse` - Datos de la respuesta

<div id="get_by_username">
  ### `get_by_username`
</div>

Obtener usuario por nombre de usuario
Recupera los detalles de un usuario específico a partir de su nombre de usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.username" type="str">
Un nombre de usuario.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`GetByUsernameResponse` - Datos de la respuesta

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

Obtener usuarios por nombres de usuario
Recupera los detalles de varios usuarios a partir de sus nombres de usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.usernames" type="List">
Una lista de nombres de usuario separados por comas.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se van a mostrar.
</ParamField>

#### Devuelve

`GetByUsernamesResponse` - datos de la respuesta

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

Obtener las Listas seguidas
Obtiene una lista de Listas que sigue un usuario específico a partir de su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista de campos de List, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos a expandir, separados por comas.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

#### Devuelve

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

Obtener seguidores
Obtiene una lista de usuarios que siguen a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Lista separada por comas de campos de Publicación para mostrar.
</ParamField>

#### Devuelve

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

Obtener cuentas que sigue
Recupera una lista de Usuarios seguidos por un Usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

Obtener Publicaciones con Me gusta
Recupera una lista de Publicaciones a las que un Usuario específico les ha dado Me gusta mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario que se desea consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de publicación para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

#### Devuelve

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

Obtener membresías en Listas
Obtiene una lista de Listas de las que un usuario específico es miembro según su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una página específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de Lista que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

Obtener mi usuario
Obtiene los detalles del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos separados por comas para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación separados por comas para mostrar.
</ParamField>

#### Devuelve

`GetMeResponse` - datos de la respuesta

<div id="get_mentions">
  ### `get_mentions`
</div>

Obtener menciones
Obtiene una lista de Publicaciones que mencionan a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más temprana a partir de la cual se devolverán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se devolverán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

Obtener usuarios silenciados  
Obtiene una lista de usuarios silenciados por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
La id del Usuario de origen autenticado para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente página de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de Usuario separados por comas que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos separados por comas que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación separados por comas que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

Obtener Listas propias
Obtiene una lista de Lists que son propiedad de un Usuario específico identificado por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario que se desea consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una página determinada de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de List que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

Obtener Listas fijadas
Devuelve una lista de Listas fijadas por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del User autenticado de origen para el que se devolverán resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de List que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se deben expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

#### Devuelve

`GetPinnedListsResponse` - Datos de la respuesta

<div id="get_posts">
  ### `get_posts`
</div>

Obtener publicaciones
Obtiene una lista de publicaciones creadas por un usuario específico a partir de su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se excluirán (p. ej., "replies" o "retweets").
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se proporcionarán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de Publicación que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista, separada por comas, de campos que se ampliarán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de contenido multimedia que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de encuesta que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de lugar que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

Obtener mis Reposts
Obtiene una lista de Publicaciones que son Reposts del contenido del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente página de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

#### Devuelve

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

Obtener la cronología
Recupera una lista de Publicaciones en orden cronológico inverso en la cronología del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado cuyas Publicaciones de la cronología inversa se van a listar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se deben excluir (por ejemplo, «replies» o «retweets»).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se proporcionarán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

Indicar "Me gusta" en una Publicación
Hace que el usuario autenticado indique "Me gusta" en una Publicación específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que solicita dar Me gusta a la Publicación.
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
Cuerpo de la solicitud
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

Silenciar usuario
Hace que el usuario autenticado silencie a un usuario específico según su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que solicita silenciar al usuario de destino.
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
Cuerpo de la solicitud
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

Anclar Lista
Hace que el usuario autenticado ancle una Lista específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que fijará la Lista.
</ParamField>

<ParamField path="body.body" type="PinListRequest">
Cuerpo de la solicitud
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

Volver a publicar Publicación
Hace que el usuario autenticado vuelva a publicar una Publicación específica según su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario autenticado de origen que solicita volver a publicar la Publicación.
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
Cuerpo de la solicitud
</ParamField>

<div id="search">
  ### `search`
</div>

Buscar usuarios
Obtiene una lista de usuarios que coinciden con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="Any">
La cadena de consulta con la que se buscará a los usuarios.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados. El valor usado con el parámetro se toma directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos que se van a expandir, separados por comas.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

Desbloquear MD
Desbloquea los mensajes directos de o hacia un usuario específico, identificado por su id, para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de destino al que el usuario autenticado solicita desbloquear los Mensajes Directos (DM).
</ParamField>

#### Devuelve

`UnblockDmsResponse` - Datos de la respuesta

<div id="unfollow_list">
  ### `unfollow_list`
</div>

Dejar de seguir una Lista
Hace que el usuario autenticado deje de seguir una Lista específica identificada por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que dejará de seguir la Lista.
</ParamField>

<ParamField path="path.list_id" type="Any">
El id de la Lista que se dejará de seguir.
</ParamField>

#### Devuelve

`UnfollowListResponse` - datos de la respuesta

<div id="unfollow_user">
  ### `unfollow_user`
</div>

Dejar de seguir a un usuario
Hace que el usuario autenticado deje de seguir a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.source_user_id" type="Any">
El id del Usuario de origen autenticado que solicita dejar de seguir al Usuario de destino.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
El id del Usuario al que el Usuario de origen solicita dejar de seguir.
</ParamField>

#### Devuelve

`UnfollowUserResponse` - datos de la respuesta

<div id="unlike_post">
  ### `unlike_post`
</div>

Quitar Me gusta de la Publicación
Hace que el usuario autenticado quite el Me gusta de una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado que solicita quitar el Me gusta de la Publicación.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
El ID de la Publicación de la que el Usuario solicita quitar el Me gusta.
</ParamField>

#### Devuelve

`UnlikePostResponse` - datos de la respuesta

<div id="unmute_user">
  ### `unmute_user`
</div>

Quitar silencio de usuario
Hace que el usuario autenticado quite el silencio a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.source_user_id" type="Any">
El ID del usuario de origen autenticado que solicita dejar de silenciar al usuario de destino.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
El ID del usuario al que el usuario de origen solicita dejar de silenciar.
</ParamField>

#### Devuelve

`UnmuteUserResponse` - datos de la respuesta

<div id="unpin_list">
  ### `unpin_list`
</div>

Desanclar una Lista
Hace que el usuario autenticado desancle una Lista específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.list_id" type="Any">
El id de la Lista que se desanclará.
</ParamField>

#### Devuelve

`UnpinListResponse` - Datos de respuesta

<div id="unrepost_post">
  ### `unrepost_post`
</div>

Quitar republicación de una Publicación
Hace que el usuario autenticado deje de republicar una Publicación específica por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que solicita volver a publicar la Publicación.
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
El id de la Publicación cuya republicación el Usuario solicita anular.
</ParamField>

#### Devuelve

`UnrepostPostResponse` - Datos de respuesta