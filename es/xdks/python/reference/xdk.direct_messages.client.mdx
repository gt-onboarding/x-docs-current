---
title: "DirectMessagesClient"
sidebarTitle: "DirectMessagesClient"
---

<div id="directmessagesclient">
  ## DirectMessagesClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Base: object</Badge>

Cliente para operaciones con mensajes directos

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_by_conversation_id">
  ### `create_by_conversation_id`
</div>

Crear un mensaje directo por id de conversación
Envía un nuevo mensaje directo a una conversación específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.dm_conversation_id" type="str">
El ID de la conversación de Mensajes Directos.
</ParamField>

<ParamField path="body.body" type="CreateByConversationIdRequest">
Cuerpo de la solicitud
</ParamField>

<div id="create_by_participant_id">
  ### `create_by_participant_id`
</div>

Crear mensaje directo por id de participante
Envía un nuevo mensaje directo a un participante específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.participant_id" type="Any">
La ID del usuario destinatario que recibirá el DM.
</ParamField>

<ParamField path="body.body" type="CreateByParticipantIdRequest">
Cuerpo de la solicitud
</ParamField>

<div id="create_conversation">
  ### `create_conversation`
</div>

Crear conversación de MD
Inicia una nueva conversación de mensaje directo con los participantes especificados.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateConversationResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateConversationRequest"></ParamField>

<div id="delete_events">
  ### `delete_events`
</div>

Eliminar evento de MD
Elimina un evento específico de mensaje directo por su id, siempre que pertenezca al usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.event_id" type="Any">
La id del evento de mensaje directo que se va a eliminar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteEventsResponse` - Datos de la respuesta

<div id="get_events">
  ### `get_events`
</div>

Obtiene eventos de MD
Recupera una lista de los eventos recientes de mensajes directos de todas las conversaciones.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
El conjunto de event_types que se incluirán en los resultados.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Una lista separada por comas de campos DmEvent que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se ampliarán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetEventsResponse`

<div id="get_events_by_conversation_id">
  ### `get_events_by_conversation_id`
</div>

Obtiene eventos de MD para una conversación de MD
Recupera eventos de mensajes directos para una conversación específica.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de la conversación de DM.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una página específica de resultados.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
El conjunto de event_types que se incluirán en los resultados.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Una lista separada por comas de campos de DmEvent para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de contenido multimedia para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetEventsByConversationIdResponse`

<div id="get_events_by_id">
  ### `get_events_by_id`
</div>

Obtener evento de mensaje directo por id
Obtiene los detalles de un evento de mensaje directo específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.event_id" type="Any">
id del evento de MD.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Una lista de campos de DmEvent, separados por comas, que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de contenido multimedia, separados por comas, que se van a mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, que se van a mostrar.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación, separados por comas, que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetEventsByIdResponse` - Datos de la respuesta

<div id="get_events_by_participant_id">
  ### `get_events_by_participant_id`
</div>

Obtener eventos de mensajes directos para una conversación de mensajes directos
Recupera eventos de mensajes directos para una conversación específica.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.participant_id" type="Any">
El ID del usuario participante en la conversación de DM uno a uno.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una “página” determinada de resultados.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
El conjunto de event_types que se incluirán en los resultados.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Una lista separada por comas de campos de DmEvent que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Publicación que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetEventsByParticipantIdResponse`