---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">Class</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones con listas

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

Agregar miembro a List
Agrega un usuario a una List específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de la List a la que se agregará un miembro.
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
Cuerpo de la solicitud
</ParamField>

<div id="create">
  ### `create`
</div>

Crear Lista
Crea una nueva Lista para el usuario autenticado.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Eliminar Lista
Elimina una Lista específica perteneciente al usuario autenticado mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la Lista que se va a eliminar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteResponse` - datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener Lista por ID
Obtiene los detalles de una Lista específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la Lista.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista de campos de la Lista, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se van a expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdResponse` - Datos de la respuesta

<div id="get_followers">
  ### `get_followers`
</div>

Obtener seguidores de la Lista
Obtiene una lista de usuarios que siguen una Lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la Lista.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

Obtener miembros de una Lista
Obtiene una lista de usuarios que son miembros de una Lista específica identificada por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la lista.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

Obtener Publicaciones de una Lista
Obtiene una lista de Publicaciones asociadas a una Lista específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de la Lista.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Publicación, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de contenido multimedia, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista de campos de encuesta, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista de campos de lugar, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

Eliminar miembro de una Lista
Elimina a un usuario de una Lista específica usando su id y la id del usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la Lista de la que se eliminará un miembro.
</ParamField>

<ParamField path="path.user_id" type="Any">
El ID del usuario que se eliminará de la Lista.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`RemoveMemberByUserIdResponse` - Datos de respuesta

<div id="update">
  ### `update`
</div>

Actualizar Lista
Actualiza los detalles de una Lista específica, que pertenece al usuario autenticado, usando su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la List que se va a modificar.
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
Cuerpo de la solicitud
</ParamField>