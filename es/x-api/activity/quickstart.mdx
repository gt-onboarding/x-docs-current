---
title: Inicio rápido
sidebarTitle: Inicio rápido
keywords: ["inicio rápido de la Activity API", "inicio rápido de la X Activity API", "inicio rápido de actividad", "tutorial de actividad", "guía de actividad"]
---

Esta guía explica cómo suscribirte y recibir eventos usando los endpoints de la X Activity API. Por lo general, consta de 3 pasos:

1. Identificar la id de usuario del usuario cuyos eventos quieres filtrar
2. Crear una suscripción para el tipo de evento que quieres filtrar para ese usuario
3. Recibir los eventos mediante un webhook o una conexión HTTP persistente de streaming

<div id="getting-user-ids">
  ## Obtención de IDs de usuario
</div>

Antes de crear suscripciones, necesitas conocer el ID de usuario de la cuenta que quieres filtrar. En este ejemplo, usaremos el usuario @XDevelopers. Puedes obtener IDs de usuario de varias formas, entre ellas:

**Buscar el ID de un usuario por nombre de usuario:**

```bash
curl -H "Authorization: Bearer YOUR_BEARER_TOKEN" "https://api.x.com/2/users/by/username/xdevelopers"
```

**Obtén tu id de usuario:**

```bash
curl -H "Authorization: Bearer YOUR_BEARER_TOKEN" https://api.x.com/2/users/me
```

Ambos endpoints devuelven información del usuario, incluido el campo `id`, que puedes utilizar en filtros de suscripción. Se muestra a continuación un ejemplo de respuesta JSON:

```json
{
    "data": {
        "id": "2244994945",
        "name": "Developers",
        "username": "XDevelopers"
    }
}
```


<div id="creating-a-subscription">
  ## Creación de una suscripción
</div>

El siguiente paso es crear una suscripción. En este ejemplo, nos suscribiremos a las actualizaciones de la biografía de XDevelopers. Para ello, pasaremos el `user_id` y el `event_type` en el cuerpo JSON. En este caso, el `event_type` es `ProfileBioUpdate`.

Pasaremos el ID de usuario de X Developer: `2244994945`, y una etiqueta opcional:

```json
{
  "event_type": "ProfileBioUpdate",
  "filter": {
    "user_id": "2244994945"
  },
  "tag": "Xdevelopers' bio updates"
}
```

Utilizaremos nuestro [Bearer Token](https://docs.x.com/fundamentals/authentication/oauth-2-0/overview#bearer-token-also-known-as-app-only) (obtenido en el portal para desarrolladores) para la autorización en todos los endpoints relacionados con XAA:

```bash
curl -H "Authorization: Bearer YOUR_BEARER_TOKEN" \
  https://api.x.com/2/activity/subscriptions \
  -X POST \
  -d '{
    "event_type": "ProfileBioUpdate",
    "filter": {
      "user_id": "2244994945"
    },
    "tag": "Xdevelopers' bio updates"
  }'
```

Si la solicitud se realiza correctamente, se creará tu suscripción:

```json
{
  "data":[
    {
      "created_at":"2025-10-09T16:35:08.000Z",
      "event_type":"ProfileBioUpdate",
      "filter":{
        "user_id":"2244994945"
      },
      "subscription_id":"1976325569252868096",
      "tag": "Actualizaciones de biografía de Xdevelopers",
      "updated_at":"2025-10-09T16:35:08.000Z"
    }
  ],
  "meta": {
    "total_subscriptions": 1
  }
}
```


<div id="getting-the-events">
  ## Cómo obtener los eventos
</div>

Una vez creada la suscripción, podemos recibir eventos mediante [webhooks](https://docs.x.com/x-api/webhooks/introduction) o un stream HTTP persistente. En este ejemplo, abriremos el stream HTTP persistente:

```bash
curl -H "Authorization: Bearer YOUR_BEARER_TOKEN" https://api.x.com/2/activity/stream
```

Cuando la cuenta de Xdevelopers actualice la biografía de su perfil, el evento se enviará a través del stream:

```json
{
  "data": {
    "filter": {
      "user_id": "2244994945"
    },
    "event_type": "ProfileBioUpdate",
    "tag": "Actualizaciones de biografía de Xdevelopers",
    "payload": {
      "before": "Mars & Cars",
      "after": "Mars, Cars & AI"
    }
  }
}
```


<div id="subscription-management">
  ## Gestión de suscripciones
</div>

La API de actividad de X proporciona endpoints para administrar suscripciones mediante operaciones CRUD estándar.

<div id="create-subscription">
  ### Crear una suscripción
</div>

Crea una nueva suscripción para recibir eventos:

```bash
curl -H "Authorization: Bearer YOUR_BEARER_TOKEN" \
  -X POST \
  https://api.x.com/2/activity/subscriptions \
  -d '{
    "event_type": "ProfileBioUpdate",
    "filter": {
      "user_id": "123456789"
    },
    "tag": "my bio updates",
    "webhook_id": "1976325569252868099"
  }'
```

**Nota:**

* El campo `tag` es opcional. Puede utilizarse para ayudar a identificar eventos en el momento de la entrega.
* El campo `webhook_id` también es opcional. Consulte nuestra [documentación de webhooks](https://docs.x.com/x-api/webhooks/introduction) para obtener ayuda para configurar un webhook. Si se especifica un `webhook_id`, el evento se entregará al webhook proporcionado, además del stream si está abierto.

**Respuesta:**

```json
{
  "data": {
    "subscription_id": "1976325569252868096",
    "event_type": "ProfileBioUpdate",
    "filter": {
      "user_id": "123456789"
    },
    "created_at": "2025-10-09T16:35:08.000Z",
    "updated_at": "2025-10-09T16:35:08.000Z",
    "tag": "my bio updates",
    "webhook_id": "1976325569252868099"
  }
}
```


<div id="list-subscriptions">
  ### Listar suscripciones
</div>

Obtén todas las suscripciones activas de tu aplicación:

```bash
curl -H "Authorization: Bearer YOUR_BEARER_TOKEN" \
  https://api.x.com/2/activity/subscriptions
```

**Respuesta:**

```json
{
  "data": [
    {
      "subscription_id": "1976325569252868096",
      "event_type": "ProfileBioUpdate",
      "filter": {
        "user_id": "123456789"
      },
      "created_at": "2025-10-09T16:35:08.000Z",
      "updated_at": "2025-10-10T03:50:59.000Z",
    },
    {
      "subscription_id": "1976325569252868097",
      "event_type": "ProfilePictureUpdate",
      "filter": {
        "user_id": "987654321"
      },
      "created_at": "2025-10-08T14:35:08.000Z",
      "updated_at": "2025-10-08T14:35:08.000Z",
    }
  ],
  "meta": {
    "total_subscriptions": 2
  }
}
```


<div id="delete-subscription">
  ### Eliminar suscripción
</div>

Eliminar una suscripción:

```bash
curl -H "Authorization: Bearer YOUR_BEARER_TOKEN" \
  -X DELETE \
  https://api.x.com/2/activity/subscriptions/1976325569252868096
```

**Respuesta:**

```json
{
  "data": {
    "deleted": true
  },
  "meta": {
    "total_subscriptions": 0
  }
}
```

`total_subscriptions` muestra la cantidad de suscripciones restantes asociadas a tu aplicación tras la operación de eliminación.


<div id="update-subscription">
  ### Actualizar suscripción
</div>

El endpoint PUT te permite actualizar el método de entrega o la etiqueta de una suscripción.

Actualizar el `filter` o el `event_type` requiere eliminar la suscripción existente y crear una nueva.

```
curl -H "Authorization: Bearer YOUR_BEARER_TOKEN" \
  -X PUT \
  https://api.x.com/2/activity/subscriptions/1976325569252868096 \
  -d '{
    "tag": "my new tag",
    "webhook_id": "192846273860294839"
  }'
```

**Respuesta:**

```json
{
  "data": {
    "subscription_id": "1976325569252868096",
    "event_type": "ProfileBioUpdate",
    "filter": {
      "user_id": "123456789"
    },
    "created_at": "2025-10-09T16:35:08.000Z",
    "updated_at": "2025-10-10T17:10:58.000Z",
    "tag": "my new tag",
    "webhook_id": "192846273860294839"
  },
  "meta": {
    "total_subscriptions": 1
  }
}
```
