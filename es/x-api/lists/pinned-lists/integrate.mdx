---
title: Guía de integración
sidebarTitle: Guía de integración
keywords: ["integración de listas fijadas", "integración para fijar listas", "guía sobre listas fijadas", "configuración de listas fijadas", "integración del fijado de listas"]
---

import { Button } from '/snippets/es/button.mdx';

Esta página contiene información sobre varias herramientas y conceptos clave que debes conocer al integrar los endpoints de mutes en tu sistema. Hemos dividido la página en varias secciones:

* [Herramientas útiles](#helpful)
* Conceptos clave
* [Autenticación](#authentication)
* [Developer portal, Proyectos y Aplicaciones](#portal)
* [Límites de frecuencia](#limits)
* [Campos y expansiones](#fields)

<div id="helpful-tools">
  ### Herramientas útiles
</div>

Antes de entrar en algunos conceptos clave que te ayudarán a integrar este endpoint, te recomendamos familiarizarte con:

<div id="postman">
  #### Postman
</div>

Postman es una excelente herramienta que puedes usar para probar un endpoint. Cada solicitud de Postman incluye todos los parámetros de la ruta y del cuerpo para ayudarte a comprender rápidamente qué está disponible para ti. Para obtener más información sobre nuestras colecciones de Postman, visita nuestra página [&quot;Uso de Postman&quot;](/es/tutorials/postman-getting-started). 

<div id="code-samples">
  #### Ejemplos de código
</div>

Si quieres empezar a usar este endpoint con código en tu lenguaje de programación preferido, tenemos varios ejemplos de código disponibles que puedes usar como punto de partida en nuestra [página de GitHub](https://github.com/xdevplatform/Twitter-API-v2-sample-code).

<div id="third-party-libraries">
  #### Bibliotecas de terceros
</div>

Aprovecha una de las [bibliotecas de terceros](/es/x-api/tools-and-libraries/overview) de nuestras comunidades para ayudarte a empezar. Puedes encontrar una biblioteca que funcione con los endpoints de la v2 buscando la etiqueta de versión adecuada.

<div id="key-concepts">
  ### Conceptos clave
</div>

<div id="authentication">
  #### Autenticación
</div>

Todos los endpoints de X API v2 requieren que autentiques tus solicitudes con un conjunto de credenciales, también conocidas como claves y tokens. Puedes usar el Contexto de usuario de OAuth 1.0a para autenticar tus solicitudes en este endpoint. 

Con el [Contexto de usuario de OAuth 1.0a](/es/resources/fundamentals/authentication), debes usar un conjunto de claves de API y tokens de acceso de usuario para realizar una solicitud exitosa. Los tokens de acceso deben estar asociados con el usuario en cuyo nombre estás realizando la solicitud. Si deseas generar un conjunto de tokens de acceso para otro usuario, este debe autorizar tu Aplicación utilizando el [flujo OAuth de tres patas](https://developer.x.com/resources/fundamentals/authentication/obtaining-user-access-tokens).

Ten en cuenta que OAuth 1.0a puede ser difícil de usar. Si no estás familiarizado con este método de autenticación, te recomendamos que uses una [biblioteca](/es/x-api/tools-and-libraries/overview) o una herramienta como Postman.

<div id="developer-portal-projects-and-developer-apps">
  #### Portal para desarrolladores, Proyectos y aplicaciones de desarrollador
</div>

Para obtener un conjunto de credenciales de autenticación que funcionen con los endpoints de X API v2, debes [registrarte para obtener una cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info), configurar un [Proyecto](/es/resources/fundamentals/projects) dentro de esa cuenta y crear una [aplicación de desarrollador](/es/resources/fundamentals/developer-apps) dentro de ese Proyecto. Luego podrás encontrar tus claves y tokens dentro de tu aplicación de desarrollador.
 

<div id="rate-limits">
  #### Límites de frecuencia
</div>

Cada día, muchos miles de desarrolladores realizan solicitudes a la X API. Para ayudar a gestionar el gran volumen de estas solicitudes, se aplican [límites de frecuencia](/es/x-api/fundamentals/rate-limits) a cada endpoint, que restringen la cantidad de solicitudes que puedes hacer en nombre de tu aplicación o en nombre de un usuario autenticado. 

Estos endpoints están sujetos a límites de frecuencia a nivel de usuario, lo que significa que el usuario autenticado en cuyo nombre realizas la solicitud solo puede llamar al endpoint un número determinado de veces a través de cualquier aplicación de desarrollador. 

La siguiente tabla muestra los límites de frecuencia para cada endpoint.

|     |     |     |
| :--- | :--- | :--- |
| **Endpoint** | **Método HTTP** | **Límite de frecuencia** |
| /2/users/:id/pinned&#95;lists | POST | 50 solicitudes cada 15 minutos |
| /2/users/:id/pinned&#95;lists/:list&#95;id | DELETE | 50 solicitudes cada 15 minutos |
| /2/users/:id/pinned&#95;lists | GET | 15 solicitudes cada 15 minutos |

<div id="fields-and-expansions">
  #### Campos y expansiones
</div>

El endpoint GET de X API v2 permite seleccionar exactamente qué datos se quieren obtener de la API utilizando un conjunto de parámetros llamados `fields` y `expansions`. El parámetro `expansions` permite expandir los objetos referenciados en el payload. Por ejemplo, consultar Listas fijadas permite obtener las siguientes [expansiones](/es/x-api/fundamentals/expansions):

* `owner_id`

El parámetro `fields` permite seleccionar exactamente qué [campos](/es/x-api/fundamentals/fields) dentro de los diferentes objetos de datos se desean recibir. Este endpoint devuelve principalmente objetos de usuario. De forma predeterminada, el objeto List devuelve los campos `id` y `name`. Para recibir campos adicionales como `list.created_at` o `list.description`, tendrás que solicitarlos específicamente utilizando el parámetro `fields`. 

Hemos añadido a nuestro [diccionario de datos de X API v2](/es/x-api/fundamentals/data-dictionary) una guía sobre cómo usar [campos y expansiones](/es/x-api/fundamentals/data-dictionary#how-to-use-fields-and-expansions) conjuntamente.

La siguiente tabla muestra los campos y expansiones disponibles para el endpoint de consulta:

|     |     |     |
| :--- | :--- | :--- |
| **Endpoint** | **Campos** | **Expansiones** |
| /2/users/:id/pinned&#95;lists | `list.fields`<br /><br />`user.fields` | `owner_id` |